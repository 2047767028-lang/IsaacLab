# IsaacLab 项目工作准则

> 本文件记录 Claude Code 在本项目中应遵守的工作准则和用户偏好，
> 从 `genie_sim` 项目的 `CLAUDE.md` 中提炼而来（那份文件里项目专属的进度/bug记录未搬运）。

## 项目目标

用 **Isaac Sim + Isaac Lab** 操纵**两台 Franka 机械臂**，做**家具装配**任务。

路线：**遥操作采集少量真实演示数据** → 用 **MimicGen（isaaclab_mimic）系列工具** 把少量数据
放大成大量数据 → 喂给**策略训练**。

## 主线状态（2026-09-02 更新，下次对话先看这里）

### 主线一：双臂 Franka 家具装配（原始项目目标，当前焦点）

**当前进度**：目前跑通的是官方单臂 `Isaac-Stack-Cube-Franka-IK-Rel-v0`（叠方块任务）的
record → annotate → generate 全流程，用的是**官方自带的 10 条源演示**，不是自己遥操作
采出来的数据；也还是**单臂**，不是双臂家具装配。细节见 `RUNBOOK_mimic.md`。
双臂 Franka 环境和家具装配任务/资产目前在本项目里还没有配置。
自己遥操作那次（`my_demos.hdf5`）文件损坏/未录成，还没补录。

**2026-08-27：VR 遥操作已打通** ✅ 用户实测可正常操作（画面正常、右手手柄驱动机械臂、
扳机控夹爪）。**注意跑在实验室服务器上，不是本机**：`surf2@192.168.3.254` 的
`~/dev/furniture_assembly/`，用的是 **Isaac Lab 3.0.0 + Isaac Teleop 新架构**，跟本仓库
当前分支（2.3.2 的 `teleop_devices`/`OpenXRDevice`/CloudXR docker 容器那一套）**完全不是
一回事**，别混着看。完整步骤、连接方式、操作按键、6 个踩坑记录见 `RUNBOOK_mimic.md`
"VR 遥操作 (CloudXR + Isaac Teleop)"节；服务器配置文件备份在
`docs/vr_teleop_server_config/`。

⚠️ **一条已作废的旧结论**：2.3.2 文档说 Quest 3/Pico 4 需要申请 CloudXR Early Access
——已过时，NGC 上那个资源已 404 下架，现在 `cloudxr-js` 是 GA 公开可下，客户端直接用
浏览器打开 `https://nvidia.github.io/IsaacTeleop/client`。**不要再去申请 Early Access。**

**向上游提了四个 PR**（都已核实存在于 upstream `develop`，非本地环境问题；状态 2026-09-02
核实，随时用 `gh pr list --repo isaac-sim/IsaacLab --author 2047767028-lang --state all` 重查）：
- #7381 多 GPU 机器上 CloudXR 编码器和渲染器跑到不同物理卡 → 头显里是雪花。**已合并**
  （2026-08-31）。
- #7380 CloudXR EULA 无法免交互接受 → headless/CI 启动必失败。分支
  `fix/cloudxr-eula-noninteractive`。AntoineRichard 的三条意见（replay 路径漏接、测试隔离、
  文档）8/29 已改完（`7c48a40cf`）；#7381 合并后跟 develop 冲突——两个 PR 在
  `session_lifecycle.py` 同一位置各加了一段模块级辅助函数，9/2 用 **merge develop** 的方式
  解决（`c4711125b`，两段纯新增全保留；不 rebase 是为了不 force-push 改写 PR 上的 review
  历史），`test_cloudxr_lifecycle.py` 33 个用例在 `ISAACLAB_CXR_ACCEPT_EULA` 未设/`1`/`0`
  三种取值下全过，已在 PR 回复。现在等维护者审批。
- #7433 `DataGenConfig.max_num_failures` 全仓库 18 处赋值但从未被读取（就是主线二 2.1 节
  踩的那个坑）→ 让它真正兜底。分支 `fix/mimic-max-num-failures`。两条 bot 意见已处理：
  `generation_guarantee` 门控已加（`3bcd74c09`，固定尝试次数模式不受 cap 影响）；"新文件
  license 头应该是 BSD"是**误报**——`.pre-commit-config.yaml` 对 `source/isaaclab_mimic/`
  强制 Apache-2.0 头，隔壁 `mimic_test_utils.py` 就是同款，已在 PR 里回复。等
  kellyguo11/peterd-NV 审批。
- #7434 `cubes_stacked` 只看瞬时几何构型，方块下落途中经过"叠好"的高度也算成功，而 Mimic
  生成器按"任意帧曾成功"累计，一帧误判就把整条 episode 当成功 demo 写进数据集 → 加线速度
  门控（默认 0.05 m/s，可传 `None` 关掉）。分支 `fix/cubes-stacked-at-rest`。ooctipus 要求
  删测试文件，kellyguo11 8/31 接受"不用 Kit 能快跑就留着"，9/2 删掉 `AppLauncher` 前导
  （`282d9cca0`），12 个用例 0.68 秒无 Kit 通过，已在 PR 回复。等审批。

前两个的核实过程、证据、代码定位见 `docs/upstream_bug_findings_xr_teleop.md`。

**本机怎么跑 develop 分支（3.x）的测试**：本机 conda `isaaclab` 是 2.3.2，`import isaaclab`
就要 Kit（`utils/mesh.py` 导 `pxr`），而且本地根本没有 `contrib/stack`、`isaaclab_teleop`。
办法：给 PR 分支建 worktree；`pip install --target <临时目录> lazy_loader`（develop 新依赖，
把顺带装进来的 `packaging*` 删掉，否则会压掉环境里的版本）；`PYTHONPATH` 放 worktree 下
**全部** `source/*` 包外加那个临时目录，然后
`conda run -n isaaclab python -m pytest <文件> -p no:cacheprovider`。只放 `isaaclab`/
`isaaclab_tasks` 不够——`isaaclab_assets` 会落到本机 2.3.2 版本，报
"`RigidBodyPropertiesCfg` has moved to `isaaclab_physx`"。develop 核心包已是懒加载，纯张量
逻辑的单测 1 秒内导入完、不加载任何 `omni`/`carb`/`pxr`。需要 `carb`/`pxr`/`isaacteleop` 或
真实 teleop 进程的测试文件本机跑不了，是环境限制；判断有没有回归要拿原封不动的
`origin/develop` 用同样方式跑一遍做对照，比对每个文件的通过数。develop 的 lint 是
**ruff 0.14.10 + codespell**（不是 black/isort），在 worktree 根目录跑
`uvx ruff@0.14.10 check/format --check <文件>` 就能吃到 pyproject 里的配置。

### 主线二：接触锚定的扰动增广（研究，进行中）

⚠️ **新开 session 先读 `docs/主线二_思维链交接.md`**——那份记的是**推理链条本身**（信念怎么变的、现在哪些确信哪些悬着、下一步的逻辑分叉、已走过的死路、踩过的推理陷阱），本节记的是事实和数字，两者互补。

设计文档：`docs/接触锚定扰动增广_设计记录.md`（机制推导、风险清单、写作定位）。
更细的逐日推理/踩坑全过程另外归档在`docs/主线二_设计与实验历史.md`，本节是主文档里
应该保留的完整工作记忆——**按用户明确要求，不要为了行数把这节压成只剩指针**。

一句话目标：验证 DP/ACT 策略对"视觉伺服交接给它那一瞬间"的初始位姿偏差的容忍边界，
方法是在 MimicGen 生成时给自由空间段加位姿扰动、接触段完全冻结。

**分支**：`perturbation-experiment`（从 v2.3.2 tag 切出，因为本机装的是 Isaac Lab
2.3.2，不能切去`release/3.0.0-beta2`；仓库当时是分离头指针状态，切分支是为了让自动
提交有安全落点）。

#### 2.1 起点扰动（v1）——只扰第一个subtask的起点

**关键实现发现**：MimicGen生成时第一个subtask的插值起点直接来自
`env.get_robot_eef_pose()`（钩子：`data_generator.py:538` `merge_eef_subtask_trajectory`），
而reset阶段已有现成的机械臂关节高斯噪声事件`randomize_franka_joint_state`
（`stack_joint_pos_env_cfg.py`的`EventCfg`）——参数化它的`std`即可让扰动天然流入
生成链路，完全不用碰MimicGen核心代码。`PERTURB_STD`环境变量控制幅度（弧度，加在7个
手臂关节上，不含夹爪），commit `467931872`新增env cfg+gym id
`Isaac-Stack-Cube-Franka-IK-Rel-Mimic-Perturbed-v0`。`PERTURB_SEED`（`6e5bd3404`）
控制复现性重跑的种子。

**已纠正的错误（教训）**：曾经以为`max_num_failures=25`会兜底、低成功率σ不会拖时间，
这是错的——只读了`DataGenConfig.max_num_failures`文档字符串没验证代码，实测σ=5.0跑到
337次尝试才凑够10个成功。查`generation.py`的`env_loop()`才发现这个字段全代码库18个
mimic env cfg都赋值但从未被读取，真正终止条件只有"凑够`generation_num_trials`个成功"
（默认`generation_guarantee=True`），低成功率会无界重试。用`PERTURB_FIXED_ATTEMPTS`
（commit `8c8cd05a0`）修复：设置后`generation_guarantee`切`False`，语义变成"固定跑
多少次尝试"。**教训：配置字段文档字符串是设计意图，不是实际生效行为，必须读执行代码。**

**最终扫描**：20个σ值（0.00~8.00弧度）× 5 seed × 每点固定500次尝试=5万次，实测约7小时。
**结果**（`docs/接触锚定扰动增广_实验记录_v1.md` + `docs/perturbation_sweep_v1_results/`，
commit `469bab130`）：σ=0.00~0.40成功率持平36~37%（跨5seed差异<1pp）；σ=0.5~1.6快速
下降(32.8%→10.8%)；σ≥2.0变缓，σ=5.5/8.0趋稳1.6~1.8%。**σ物理含义**：7个手臂关节独立
高斯噪声标准差，单位弧度，不是末端笛卡尔位移（之前口头说错成"含夹爪"，已订正——夹爪会
被重设回默认值不受影响）。实测末端执行器实际位移（200并行环境，reset后立刻读位姿，
σ=0.0时位置标准差仅4e-7米验证方法干净）：**σ=0.2→均值17.3cm；σ=0.4→均值35.1cm**——
"成功率几乎不掉"的平坦区已经覆盖0～十几厘米量级的初始位姿误差，不是微扰。

#### 2.2 全subtask扩展：两次失败尝试

**失败尝试①：物理瞬移**（commit `5a813b3eb`+`5f219afa7`，已revert `d9381bd46`+
`a4ebcab7e`）。触发原因：`subtask_trajectory`只由物体位姿决定跟起点扰动无关，插值一
结束就完全接回原版固定轨迹，多样性只在每段开头约5帧。方案是在subtask边界直接瞬移机器
人关节，踩出两个bug：①`get_robot_eef_pose()`读的是`obs_buf`观测缓存不是实时物理状态，
只有显式`sensor.reset()`才会下次访问重算，直接写关节状态不会触发；②更严重——
`write_joint_state_to_sim`是纯运动学瞬移不经过物理步进，摩擦力约束没时间传递，**手里
抓着的方块不会跟着瞬移，等于凭空脱手**，这是成功率暴跌到16%/0%的主因，不是"扰动幅度
大导致难恢复"。

**失败尝试②：弧线扰动v1（离散控制点+直线插值）**（commit `0d2d676a2`，已revert
`420f7d890`）。改成不碰物理仿真、纯数据层操作：从`subtask_trajectory`中间取几个控制点
加偏移（sin包络）、直线连回去。40次固定尝试成功率仅2.5%，用户追问"尾部明明收敛了为什么
成功率还这么低"，逐帧算"转向角"（相邻帧速度方向夹角）实测确认：位置数值确实收敛到0，
但控制点之间是直线段，每个控制点处（尤其弧线顶点）有真实的方向硬转折——frame 28测出
116.9°瞬间转向（基线同帧只有29.8°），跟坐标有没有对上无关，是路径本身有没有棱角的问题。
`sin(πu)`包络两端**数值**为0但**斜率**不为0，收敛边界还会叠加一次速度台阶。

**关键澄清**：用户提出"调小幅度+调高密度"，讨论后确认这不是另一个方案，是同一个方案的
极限——直线段拼出光滑曲线靠"分段越多越逼近"，密度推到极限（每帧都是控制点）就自动退化
成"逐帧连续套函数"。但密度和幅度不独立，多大密度算够取决于幅度，不能拿(密度,幅度)做
自由二维网格扫描，所以直接把密度推到理论极限，从根上消除"密度够不够"这个问题。

#### 2.3 弧线扰动 v2（逐帧连续sin²包络，当前生产在用）

commit `4ec4685e2`。不再有任何控制点/直线段，自由区间每帧直接算包络值。`sin²`比`sin`
多一层保证——两端**数值和斜率都是0**，收敛边界不会有速度台阶。`PERTURB_ARC_NUM_POINTS`
参数已删除（密度概念不再存在），只保留`PERTURB_ARC_STD`（幅度,米,>0启用）和
`PERTURB_ARC_FREEZE_FRAC`（冻结尾部比例,默认0.3）两个可调项。不碰物理仿真实时状态，
避免了2.2节两个bug——机器人手里的东西跟着真实物理连续动作走，不会被瞬移甩掉。

**v1 vs v2验证**（同seed同幅度std=0.05m）：v1成功率2.5%(1/40)→v2成功率10.0%(4/40)，
方向正确但n=40小样本统计上约90%置信不到95%。10%量级本身提示：即便棱角问题解决，4个
subtask全部叠加几厘米级弧线扰动本身代价不小（粗算单subtask约55~60%成功率四次方
0.55^4≈9.2%，与10%量级吻合）——后续更可能靠**调小幅度**换成功率，不是还有棱角要修。

**用户关键疑问：ACT会不会把不同轨迹"取均值"白费功夫？** 讨论结论（未实测，仅推理）：
大概率不会完全白费——每条扰动轨迹对应真实不同的状态（机械臂物理上就在那个偏移位置），
动作从新轨迹本身重新导出（跟设计文档2.3节"情况二"一致，自洽），不是"同一状态硬贴不同
标签"，经典"平均导致穿墙"问题大概率不出现。真正风险集中在收敛区：多条弧线收敛回同一
尾部，若收敛得晚/急，state space会重叠，重叠区"正确动作"可能因来向不同而不同——方向
一致+密度衰减这两条约束本身就在压窄这个重叠区，两件事是一体的。ACT不是纯MSE回归是
CVAE+L1，理论上能靠隐变量z区分模式但部署时z通常不采样，能不能真正分开取决于encoder学得
好不好，大概率介于"纯回归被平均坏"和"DP能吃下多模态"之间——这正是设计文档4.4节
"多模态对下游架构敏感"预判过的点，值得同时训DP和ACT对比，是有意义的发现不是要回避的
风险。**最终还是要靠训练+仿真测评本身回答，现在只是降低"这个方向完全没用"的担忧。**

**小规模探测**（11点,每点50次,freeze_frac=0.3，逐点数字见`docs/`原始记录不在此重复）：
形状是0.5~1.2cm平台区（贴基线~37%，差异在噪声内）→1.2cm后温和下降→2.5cm往后陡降。

**正式大规模扫描**（用户拍板"优先精度"后定案）：15个幅度值（cm,密集段0.5~1.5步长0.1,
稀疏延伸1.8/2.0/2.5/3.0）× 5 seed × 每点500次固定尝试=37500次，实测耗时235~238秒/点
（比模型高约13%，弧线逐帧算包络比v1单纯reset噪声多算一点），总耗时约4.9小时。**结果**
（commit `1cb5b05e0`，逐点数字见`docs/perturbation_sweep_v2_arc_results/`）：0.5cm起点
35.1%，0.5~0.9cm缓降，1.0~1.2cm小幅波动（跨seed标准差1.6~1.8pp内，判断是噪声），1.5cm
后降幅明显加大，到3.0cm降至19.1%。**3.0cm处仍在下降，没摸到"塌陷后企稳"的平台**——
以后要往更大幅度补点这个网格还没覆盖。派生数字（数学推算非实测）：假设4个subtask独立，
单subtask隐含成功率≈合并成功率^(1/4)≈74~77%，独立性假设本身没验证过。数据完整性已
独立核对（8个随机点重开hdf5一致；combined.csv恰好75行无缺失）。

#### 2.4 弧线扰动 v3（峰值位置可变，已实现，未跑规模验证）

**想法**（用户通过`/btw`命令提出，一个后台fork自动实现）：v2的`sin²(πu)`包络峰值固定
在自由区间正中央，首尾两点（自由区间起点/终点）之间的直线距离固定不受扰动影响。但峰值
出现在哪个位置是被浪费掉的自由度——可以偏早（刚离开起点就鼓出去、长距离缓慢收回）或
偏晚，不一定对称。同样的"幅度+方向"参数下能多产出一批路径形状不同的轨迹，是幅度、方向
之外的第三个多样性维度。

**实现**（commit `1706bfffc`，跟最初记录的"双段sin²拼接"数学构造不是同一个但同样成立）：
包络从固定对称的`sin²(πu)`泛化成`u^a·(1-u)^b`族（阶数a+b=6），峰值位置`u*=a/(a+b)`由
`PERTURB_ARC_PEAK_FRAC_MIN`/`_MAX`两个环境变量控制范围，每次调用独立采样。内部把峰值
位置钳制在(1/6, 5/6)保证a>1、b>1，维持v2的"两端数值和导数都精确为0"硬约束，不会重新
引入v1的转折问题。默认(0.5,0.5)锁定中心向后兼容（但函数族已换，跟v2不是逐字节相同曲线，
只是端点性质和峰值位置的定性行为一致）。

**验证情况**：只做了纯数学验证（峰值位置精确命中、端点数值和斜率趋近0），**没跑Isaac
Sim端到端验证成功率影响**——这是有意为之，超大规模仿真扫描该由协调者决定何时投入算力，
不属于那次一次性实现指令范围。**生产pi0.5训练数据时用的是v2默认行为（峰值锁中心），
v3的峰值可变能力目前完全没有被实际使用过**，下一步如果要用需要先跑小规模探测（固定/
浮动峰值对成功率的影响、跟幅度这个维度有没有交互），确认没引入新意外再考虑并入正式扫描。

#### 2.5 第二层验证启动：pi0.5训练数据交付（2026-08-22）

**图像数据缺口（阻塞性，已解决）**：之前所有σ/幅度扫描用纯状态任务（`...-Perturbed-v0`），
生成的hdf5**没有任何图像观测**（`obs`只有`eef_pos`/`joint_pos`等状态量）——pi0.5是
视觉-语言-动作模型，训不了。**解法**：仓库本来就有配套的、已注册的带摄像头变体
`Isaac-Stack-Cube-Franka-IK-Rel-Visuomotor-Mimic-v0`（`wrist_cam`+`table_cam`,各200×200
RGB uint8），弧线扰动机制写在通用MimicGen生成代码里不绑定env cfg类，直接换`--task`配合
`PERTURB_ARC_STD`不用写新代码就能生成带图像数据。冒烟测试验证图像渲染正常（人工看过
截图，机械臂和方块清晰可见，不是黑屏/花屏）。

**仿真评测基础设施现状（当时的记录，格式转换缺口后来在2.6节补上了）**：场景资产有；
`scripts/imitation_learning/robomimic/`下有评测脚本(`play.py`/`robust_eval.py`)但**只认
robomimic自己训练的policy checkpoint，不支持pi0.5**；仓库搜不到任何openpi/pi0/
policy_server代码，**没有现成的pi0.5仿真评测桥接**，需要仿照genie_sim项目那套policy
server模式自己搭，目前仍是空白（暂不阻塞训练，详见2.8节）。

**OpenPI/pi0.5数据量研究**（查了`huggingface/lerobot`官方仓库issue和`AGENT_GUIDE.md`，
不是转述）：LeRobot官方维护者对"pi0.5微调简单PnP需要多少数据"的原话——"~50 episodes
is a reasonable start for a single-location PnP; go to 100-200 if you vary
object/position."（[issue #2378](https://github.com/huggingface/lerobot/issues/2378)）。
我们的任务属于"vary object/position"，官方建议100~200条。`AGENT_GUIDE.md`7.3节训练
配置表以"~50条"为基准定pi0/pi05训练步数（batch 1~4，30k~80k步，显存受限需梯度累积）。
**结论：数据量不用追求越多越好，边际收益递减**，用户认可后定了300条/组目标（不再沿用
832条那个量级）。

**渲染并行度测试**：渲染版本比纯状态生成慢约20倍（0.4s→9.7s/次尝试，num_envs=10，GPU
渲染；CPU渲染更慢，曾经CPU渲染3分钟才过场景加载就换成GPU）。测过num_envs=24，显存到
11.3GB/12.3GB（92%），前47次尝试更快（约7.7s/次）但**之后卡死6分多钟零进展**，同时GPU
仍85%利用率、CPU仍182%——吃资源但不推进，性质类似SIGPIPE卡死信号但这次没有
subprocess.PIPE那层，根因没查清楚。**已放弃更高并行度，正式生产统一用num_envs=10。**

**执行中踩到的bug（已修复）**：`Isaac-Stack-Cube-Franka-IK-Rel-Visuomotor-Mimic-v0`是
独立于`FrankaCubeStackIKRelPerturbedMimicEnvCfg`的另一个类，从没接过`PERTURB_STD`/
`PERTURB_FIXED_ATTEMPTS`——冒烟测试复现了"无界重试"（`--generation_num_trials 20`跑到
30+还不停），且"基线"组本该是`PERTURB_STD=0`纯净对照，实际会静默带着0.02关节噪声。只有
`PERTURB_ARC_STD`/`PERTURB_ARC_FREEZE_FRAC`是通用代码里读的没受影响（符合最初设计
预期）。**修法**：新建`FrankaCubeStackIKRelVisuomotorPerturbedMimicEnvCfg`+新gym id
`Isaac-Stack-Cube-Franka-IK-Rel-Visuomotor-Mimic-Perturbed-v0`（未改动任何已有注册），
commit `02ed4b14e`。踩出bug那次冒烟测试输出直接删了没用，重新验证修复生效（精确停在
目标次数）才继续。修复后渲染吞吐7.7秒/次尝试，比预估9.7s快。

**轨迹平滑度复查**（用带图像正式task独立重测）：目标轨迹数学上仍平滑（`sin²`包络两端
数值导数都是0），但用更严格的"转角序列自身有没有孤立尖峰"指标查，发现实际执行轨迹有
局部转角尖峰（某帧99°明显高于邻居帧）——判断是**执行层面**的（1.2cm偏移塞进40~50帧
短subtask段，differential IK跟踪较绕的路径未必能平滑跟随），不是v1那种设计层面硬棱角
复发。参照点：baseline demo自身自然转角也到156.8°，99°没超过这个自然幅度，只是局部
频率更高，记录供解读训练结果参考，没有叫停生产任务。

**最终交付**（`PERTURB_FIXED_ATTEMPTS=1`模式，每组预算1050次尝试，num_envs=10）：

| | baseline (`PERTURB_ARC_STD=0`) | arc_1p2cm (`PERTURB_ARC_STD=0.012`) |
|---|---|---|
| 成功demo | 380 | 358 |
| 失败demo | 670 | 692 |
| 耗时 | 2.02h | 2.14h |
| 磁盘 | 24GB | 24GB |

两组均超300条目标，总耗时约4.16小时（12小时预算内）。数据完整性已独立复核（重开hdf5数
demo数一致；额外抽查首帧+中间帧图像像素统计均非退化）。交付说明：
`docs/pi05_training_data_v1_交付说明.md`（commit `456a3885e`）。原始hdf5在
`datasets/pi05_training_data_v1/{baseline,arc_1p2cm}/`（gitignore不随git走，需要用户
自己拷到服务器）。磁盘：两组合计47GB，剩余约39GB（89%已用）——够用但不宽裕。

**当前状态**：⬜ 训练在用户实验室服务器进行（本机不涉及，2026-08-23起），训练+仿真
测评结果会回答"扰动增广数据是否真的更好训"这个核心问题（见下面成功判据第二层）。v3
峰值可变能力已实现但未验证过对成功率的影响。robomimic→LeRobot格式转换脚本已完成并
验证（见2.6节）；仿真侧pi0.5评测桥接仍是空白，训练完之后如果要在仿真里测评pi0.5，
需要现搭（见2.8节）。

#### 2.6 pi0.5训练数据SSH交付 + HDF5→LeRobot格式转换（2026-08-23）

**服务器连接与传输**：目标服务器`surf2@192.168.3.254:/home/surf2/dev/mimic_enhance/`
（有线局域网直连，非公网）。先配置SSH key免密登录（`ssh-copy-id`），再用`rsync`传输，
实测吞吐~112MB/s（千兆网络理论满速）。只传成功demo（`generated.hdf5`+`log.txt`+
`result.txt`），不传`generated_failed.hdf5`（baseline约14.5GB/arc约16GB，比成功demo
还大，训练用不到）——两组合计约17.5GB而不是全量48GB。传输后用文件数+字节数逐个核对，
本地/服务器完全一致；长跑任务照惯例走`setsid nohup`+`disown`+`Monitor`盯日志。

**踩坑：连通性探测脚本的逻辑缺陷**。用`timeout 5 bash -c "cat < /dev/tcp/host/22"`判断
端口是否开放，实际已经收到了SSH banner（证明端口是通的），但`cat`本身不会主动退出，
5秒后被`timeout`杀掉、退出码非0，导致误判成"端口不通"。**教训：这类探测脚本要看有没
有收到数据本身，不能只看退出码。**

**转换脚本三个关键发现**（`scripts/tools/convert_hdf5_to_lerobot.py`，commit `02cac5d18`，
需要用`lerobot_v21`这个conda环境执行，`isaaclab`环境没装lerobot）：

①**`actions`(7维) vs `processed_actions`(8维)选哪个当训练目标**——逐帧数值核对发现
`processed_actions`前6维精确等于`actions`前6维的一半（`-0.026784/2=-0.013392`吻合到
小数点后多位），确认`processed_actions`是环境内部按controller scale处理后真正喂给
物理引擎的量，不是模仿学习该学的目标；`actions`才是IK-Rel控制器的原始归一化输入，
也是MimicGen/robomimic惯例里policy该学的量。**结论：训练目标用`actions`。**

②**字段命名照抄openpi官方libero转换脚本**（`image`/`wrist_image`/`state`/`actions`），
使得openpi自带的`LeRobotLiberoDataConfig`+`libero_policy.py`的`LiberoInputs`/
`LiberoOutputs`可以**直接复用，不用写一行自定义policy transform代码**——查了
`libero_policy.py`源码确认`state`维度是直接透传（我们9维vs libero 8维没关系，模型
内部自动pad到统一维度），`action`维度输出时硬编码取前7维，正好匹配我们的7维action。
State构成：`eef_pos(3)+eef_quat(4)+gripper_pos(2)`=9维（末端位姿+夹爪，DROID等数据集
的经典表示）。fps=20（从`env_args`里`dt=0.01,decimation=5`反推：`1/(0.01*5)=20Hz`）。

③**lerobot_v21环境（0.3.3版本）API跟openpi官方转换脚本模板不完全一致**——官方
`convert_aloha_data_to_lerobot.py`/`convert_libero_data_to_lerobot.py`用的是
`from lerobot.common.datasets.lerobot_dataset import ...`（`common`路径）、
`save_episode(task=task)`；本机`lerobot_v21`实际是`lerobot.datasets.lerobot_dataset`
（无`common`层）、`add_frame(frame, task=..., timestamp=None)`——`task`是`add_frame`
的独立参数不是frame字典里的键，`save_episode()`不再接受task参数。**教训：官方示例
脚本可能针对更早的lerobot commit写的，必须用`inspect.signature`核实本机实际安装版本
的API签名，不能直接照抄。** 确认`CODEBASE_VERSION="v2.1"`跟openpi `pyproject.toml`
锁定的commit（`0cf864870cf29f4738d3ade893e6fd13fbd7cdb5`）匹配，磁盘格式层面兼容，
只是Python API路径变了。

**验证**：先5条demo做smoke test，重新加载核对state/action数值精确匹配、图片逐字节
diff=0；确认无误后转全量——baseline 380 episodes/92606帧、arc_1p2cm 358 episodes/
87549帧，episode数和帧数跟源hdf5的`total`属性完全一致；用`episode_data_index`做O(1)
定位抽样核对（8个随机点，含episode边界帧），state/action/双摄像头图像全部逐字节匹配。

**磁盘空间管理**：本地磁盘一直紧张（转换前只剩39GB，baseline转换后单组占15GB）。采用
"转一组→验证→rsync传服务器→删本地副本→再转下一组"的顺序流程，不同时转换/同时留存
两组，避免磁盘写满。**当前状态**：两组LeRobot数据集已传至服务器
`lerobot_datasets/{pi05_lerobot_baseline,pi05_lerobot_arc_1p2cm}/`并核对字节数一致，
本地副本已清理。

#### 2.7 pi0.5微调超参数选型（2026-08-23）

**官方openpi训练配置调研**（curl+grep核实源码，非转述）：`TrainConfig`默认值
（`config.py:488-518`）——`lr_schedule=CosineDecaySchedule(warmup_steps=1000,
peak_lr=2.5e-5,decay_steps=30000,decay_lr=2.5e-6)`，`optimizer=AdamW(b1=0.9,b2=0.95,
weight_decay=1e-10,clip_gradient_norm=1.0)`，`ema_decay=0.99`，`batch_size=32`，
`num_train_steps=30000`。显存门槛（README表格）：LoRA微调>22.5GB(RTX 4090)，全参数
微调>70GB(A100/H100)。三个官方pi0.5示例config对比：`pi05_libero`（大规模基准，
batch=256/steps=30000）、`pi05_aloha_pen_uncap`（自定义小型LeRobot数据集全参数微调，
batch=64/steps=20000/其余默认值，**跟我们场景最像**）、`pi05_droid_finetune`（自定义
小型DROID格式，batch=32/steps=20000）。

**硬件条件描述在对话中途变化**：最初"四台5090单台32G"→后来澄清"两张被其他进程占据"，
实际可用卡数降到2或1。全参数微调门槛>70GB，2×32GB=64GB未必够——FSDP分片的是参数/
梯度/优化器状态不是激活值,"总门槛/卡数"这种简单除法不严谨,没有实测把握,该冒烟测试
而非空算。

**关键教训——LoRA没有像预期一样提速**：实测LoRA配置下`batch=32/steps=20000`单卡依然
预估32小时，跟全参数微调预估耗时接近。**原因**：LoRA节省的是显存和需要更新的参数量
（不用为全部~3B参数存AdamW一二阶动量），但训练wall-clock时间的大头是前向传播（视觉
编码器+语言模型backbone全量计算，LoRA和全参数完全一样）和反向传播（即使大部分权重
冻结，梯度仍要沿计算图传到LoRA层，这部分反传计算量基本不变）。**结论：LoRA主要省
显存不省时间**，真正该做的是用LoRA腾出来的显存空间去试更大的batch size（配合减少
`num_train_steps`维持`batch×steps`总吞吐量不变），而不是想当然假设"换LoRA就会更快"。

**两条必须同步调整的联动关系**：①`batch_size×num_train_steps`大致对应"模型总共见过
多少样本"，改小batch必须相应放大步数维持训练强度，反之亦然。②`lr_schedule.decay_steps`
定义"整条cosine退火曲线要走多少步才能降到最低点"，是独立于`num_train_steps`的参数——
只砍`num_train_steps`不改`decay_steps`，训练会在退火曲线走了一小截时被叫停，模型全程
带着接近peak的高学习率，没进入真正收敛的精细调整阶段，两组观测到的差异会混入更多
"没训稳"的噪声。**做法：把`decay_steps`设成等于新的`num_train_steps`**，配合按比例
缩小`warmup_steps`。

**控制变量硬约束（贯穿本节反复强调的一条线）**：baseline和arc_1p2cm两个`TrainConfig`
除`name`/`data.repo_id`外必须逐字段完全相同（batch_size、lr_schedule全部子字段、
num_train_steps、optimizer、ema_decay），否则观测到的差异说不清是数据还是超参造成的。

**关于"pilot版本砍训练强度是否可行"**：用户已选择"快速pilot，先看方向"（接受可能看不
出信号的风险，换几小时内拿到初步结果）。**没有一个能推算出的"安全阈值"**——砍
`num_train_steps`越狠，两组模型都欠拟合、"看不出差异"的风险越大，且无法区分"数据真的
没差别"还是"都没训够"，报告结论时必须带上这条局限，不能说成确定性结论。

#### 2.8 第二层验证的方法规划（2026-08-23 讨论；执行结果见 2.9）

**norm stats计算**（openpi训练前置步骤，遇到过"找不到norm stats"报错）：
`scripts/compute_norm_stats.py --config-name <name>`，纯统计脚本（遍历数据集算
`state`/`actions`均值方差存到`assets_dirs/repo_id`），无主观判断空间，可以放心交给
其他执行者（如服务器上另一个AI agent）代跑，但有两个前置条件：①对应的`TrainConfig`
必须已经写进`config.py`并注册到`_CONFIGS`列表，否则报"config not found"；②baseline
和arc_1p2cm两组`repo_id`不同、输出路径不同，**必须各自单独跑一次**，不能只跑一个当
共用。

**最有说服力的评测指标设计（尚未实现，方案讨论阶段）**：不是简单看训练/验证loss（loss
低不代表最终行为策略更好，模仿学习有过拟合陷阱），核心应该是**在仿真里让训练好的policy
跑闭环rollout，统计不同初始位姿扰动幅度下的任务成功率，画"成功率 vs 扰动幅度"曲线，
baseline模型和arc_1p2cm模型各一条**——直接对应研究假设（扰动增广训练出的策略对初始
偏差更鲁棒），曲线在扰动增大时下降得越慢说明越鲁棒。协议要点：①零扰动下的成功率作为
基线校验点（预期两条曲线起点接近）；②复用生成阶段已验证过的扰动机制（弧线扰动/σ扫描
代码）在同一组扰动幅度网格上分别对两个模型跑rollout；③**曲线形状（衰减速度）比单点
数字更重要**，不是比较某个幅度下谁高零点几个百分点；④可选辅助指标——失败模式细分
（抓空/堆歪/夹爪没夹住）、恢复所需额外时间/步数。

**现状（2026-08-29 更新，原先写的"完全是空白"已作废）**：桥接已建成并完整跑通，全部结果见下面 2.9 节。

**关于开源/PR的讨论（2026-08-22，用户是大三学生，目标是实习+论文，手上有G1/G2/
RealMan双臂/Franka等多机器人资源）**：建议开源独立repo现在就做（不依赖后续结果，
现在就能用），**PR到IsaacLab等训练+测评结果出来再提**（没有效果数字的PR说服力弱，
NVIDIA审核周期本来就不快）。对"实习"这个目标，现在这套工程过程（发现真实限制→
三版迭代→每次都抓出真bug并验证修复→数字驱动决策）本身就有展示价值，不用等结果。
对"论文"这个目标要诚实：这个方向不是全新的（HumanoidMimicGen论文已经验证"生成时加
噪声提升下游策略"这个大方向），单纯"改进了MimicGen"的卖法会弱，要按设计文档"用它量
不卖它"的思路——产出是"刻画了失效边界的形状"不是"发明了新方法"。多机器人资源是真实
加分项（跨embodiment characterization比单机器人单任务更硬），但规模和时间线大得多，
建议先把当前单任务的完整故事跑完（训练→测评→出结果）再决定要不要扩展到多机器人。


#### 2.9 第二层验证：完整结果（2026-08-29，本节是主线二当前最重要的结论）

**一句话结论：修好训练之后，扰动增广数据既没测出害处也没测出益处；先前"有害11.3pp"
的结论已作废，其真实来源是未收敛训练的随机波动。**

##### 2.9.1 仿真评测桥接（已建成，2.8节说的"完全是空白"作废）

`scripts/imitation_learning/openpi/play_policy_server.py`（commit `252928648` 起，
后续 `4d039b01d`/`43b8b0bbd`/`365114310` 迭代）。openpi 自带 `openpi-client` 包
(`websocket_client_policy.WebsocketClientPolicy`) + `serve_policy.py`，不用自己发明
协议。三个必踩的坑：

- **`serve_policy.py` 按名字查 `_CONFIGS` 注册表**，而我们的训练配置是
  `dataclasses.replace()` 程序化构造的、从未注册 → `--policy.config=xxx` 必然失败。
  解法：`serve_pi05_checkpoint.py` 重建同一个 config 对象直接传给
  `create_trained_policy`，不走注册表。
- **checkpoint 里 `assets/norm_stats.json` 是平铺的，加载时却按
  `assets/<asset_id>/norm_stats.json` 找** → `FileNotFoundError`。每次训练完都要补
  建嵌套路径（各流水线脚本已自动做）。
- **评测脚本跑完不退出**，卡死在 `env.close()`/`simulation_app.close()`，实测完成
  后仍吃 CPU 40+ 分钟、占着 4 张卡显存、SIGTERM 杀不掉。串行评测多个模型的驱动脚本
  会被第一个卡死。解法：结果已落盘后直接 `os._exit(0)`（commit `365114310`）。

**评测协议**：`Isaac-Stack-Cube-Franka-IK-Rel-Visuomotor-v0`，n=300 rollout，
horizon=400，`--reset_joint_std 0.0`，两模型共用 `--seed 101`。

##### 2.9.2 三个已验证的环境事实（都是实测，不是推理）

- **评测环境的方块随机化与生成训练数据时完全一致**：评测用的
  `FrankaCubeStackVisuomotorEnvCfg` 正是生成用的 `...VisuomotorPerturbedMimicEnvCfg`
  的直接父类，中间两层只改 `datagen_config` 和关节噪声 std，全仓库搜索确认
  `randomize_cube_positions` 在 Franka 链上只定义一次、从未被覆盖。
- **`--seed` 钉的是"一串各不相同场景"的可复现序列，不是单个场景**（实测：seed=101
  连续 5 次 reset 方块位置最大差 13.35cm；同 seed 重播逐位一致；换 seed 序列不同）。
  所以**多 seed 评测不增加统计效力**（5seed×60 等价 1seed×300）；两模型共用 seed 是
  为了配对比较、消掉"某些摆放本身就难"的方差。验证脚本
  `scripts/imitation_learning/openpi/verify_scene_seeding.py`。
- **训练数据生成时 `PERTURB_STD=0`**（实测：所有 demo 第一帧 `joint_pos_rel` 精确为
  0，`eef_pos` 跨 demo 标准差仅 1e-6；对照组方块位置有真实散布证明第一帧确是 reset
  后状态）。而评测环境默认硬编码 0.02 → 评测脚本加了 `--reset_joint_std` 参数并默认
  0.0 对齐训练条件（commit `43b8b0bbd`）。

##### 2.9.3 v1 结论作废：学习率全程不退火（最重要的教训）

v1 训练用了 `CosineDecaySchedule(warmup_steps=0, peak_lr=5e-5, decay_steps=10,
decay_lr=5e-5)`——**`peak_lr == decay_lr` 使余弦从 5e-5 退到 5e-5，等于恒定学习率**，
且是 openpi 默认峰值的 2 倍、无 warmup。日志里 openpi 自己打了
`this results in a constant schedule` 但没人注意。

后果实测：
- `param_norm` 四个 5000 步窗口的增量恒为 +1.208/+1.204/+1.193/+1.195（末段/首段
  = **0.989**），到第 20000 步权重仍以第 0 步的速度在漂——**从未进入收敛阶段**。
- 直接测量：**同一次训练、同一批数据，step 16000 与 19999 两个 checkpoint 的闭环
  成功率差 22.5pp**（58.5% vs 81.0%，置信区间完全不重叠）。

而当时被当作结论的数据集差异只有 11.3pp——**训练内波动是它的两倍**。所以
`baseline 81.0% vs arc 69.7%、McNemar p=0.0011` 这个"扰动增广有害"的结论**作废**。

⚠️ 讽刺的是 CLAUDE.md 2.7 节当时已经写下正确做法（"把 decay_steps 设成等于新的
num_train_steps"），但没有落实到实际训练脚本里。

**修复**：`scripts/tools/train_pi05_lora.py`（commit `91e9a3b93`），
`warmup=500 / peak=2.5e-5 / decay_steps=num_train_steps / floor=2.5e-6`。修复后
`param_norm` 末段/首段降到 **0.068~0.073**（五次训练全部落在这个窄带），末 1000 步
loss 斜率 |t|<2 已走平。

⚠️ **openpi 的 `decay_steps` 是含 warmup 的总跨度**，内部会减掉 warmup（用 3 步做
冒烟测试时算出 3-1000=-997 直接报错）。

##### 2.9.4 v2 完整结果（退火调度，10000步，超参逐字段相同）

| 数据集 | 训练种子42 | 训练种子777 | 种子平均 |
|---|---|---|---|
| baseline（380 干净） | 70.67% | 67.00% | **68.83%** |
| arc（358 全扰动 1.2cm） | 65.33% | 68.67% | **67.00%** |
| mixed（190干净+190arc） | **60.67%** | 训练中 | — |

各 n=300，Wilson 95% CI 宽度约 ±5pp。

**噪声底线（同数据换训练种子）**：baseline −3.67pp（p=0.359）、arc +3.33pp
（p=0.440）——**两个都不显著**，所以严格表述是"训练噪声 ≤ 约 4pp"，不能说"已测得
噪声为 3.5pp"。

**关键观察**：两个 arc 模型（65.33%/68.67%）把两个 baseline（70.67%/67.00%）夹在
中间。换个训练种子，arc 就从"比 baseline 差 5pp"变成"比 baseline 好 1.7pp"。

**baseline vs arc**：单种子 −5.33pp（p=0.167，不显著）；**种子平均后缩到
−1.83pp，95%CI [−7.11, +3.45]**。表观效应的大部分是训练随机性。

**mixed vs baseline（种子42）**：−10.00pp，**p=0.0078，是全研究唯一显著的对比**。
但 mixed 只有一个种子，而已知种子波动 ±3.7pp → 第二个种子（777）正在训练中，
结论待确认。若成立，方向是"混合比两个纯数据集都差"，与 2.9.5 的机制假设自洽。

##### 2.9.5 arc 扰动与人类演示变化的度量对比（解释为什么没效果）

用户提出"arc 本质是模拟人类采集时的路径扰动"。实测 10 条源演示（把末端轨迹变换到
物体坐标系以除掉物体位置的影响）：

| 量 | subtask1 接近红块 | subtask3 接近绿块 |
|---|---|---|
| 单条路径自然弯曲（相对自身两端直线的最大垂直偏离） | 均值 **4.79cm** | 均值 **9.33cm** |
| 跨演示残差离散度（已除掉物体位置差异） | **2.83cm** | **4.61cm** |
| **arc 峰值扰动** | **1.2cm** | **1.2cm** |

**方向性是决定性差异**：人类偏离**强方向性**——subtask3 的 10 条演示**全部向上**，
仰角 +47.2°~+67.0°，合成长度 R=0.850（各向同性随机的 R 中位数仅 0.285，经验
p=0.0000）；subtask1 R=0.725，p=0.0005。而 arc 用的是
`direction = torch.randn(3)` 归一化——**各向同性随机，约一半时间在往下扰动**。

**所以"arc 模拟人类变化"这个前提不成立**。人类那个向上的弧线不是"扰动"，它就是策略
本身（抬起来绕过去），而 **MimicGen 照搬源演示路径形状，这个抬升本来就完整保留在
baseline 数据里**（实测 144 条生成数据 98.6% 仍向上、仰角均值 +55.0°，对源演示的
+58°）。所以本实验真实的对照关系是"MimicGen 数据 vs MimicGen 数据 + 各向同性随机
噪声"，不是"MimicGen vs 人类风格"。

**MimicGen 位姿变换不会把抬升转歪**（用户曾假设）：实测全部 4 个 subtask 的
`object_ref` 方块 roll/pitch ≤ 0.014°，变换退化为纯绕世界 Z 轴旋转，**Z 分量精确
保留**，只有方位角被转。

##### 2.9.6 MimicGen 生成失败归因（1050次尝试仅36%成功，此前无人分析）

复现了环境自己的 `cubes_stacked` 判据并**先在成功文件上验证 100% 通过**才用于分类
（第一版复现 0% 通过，暴露两个坑：`gripper_pos` 观测里**第二根手指是取负存储的**；
成功判定要按"任意帧曾满足"而非"末帧满足"，因为约 6% 的成功案例判定后方块还会微动）。

| 失败模式 | baseline | arc |
|---|---|---|
| 抓起红块但没叠上蓝块 | 55.8% | 56.4% |
| 红块叠好但第二次抓取整个失败 | 28.2% | 26.7% |
| 几何已叠好但夹爪判据没过 | 11.3% | 11.8% |
| 抓起绿块但没叠好 | 3.4% | 3.0% |
| 没抓起红块 | 1.2% | 1.9% |

- **主导失败是真实物理失败，判据阈值没问题**：末帧 cube2-cube1 距离呈**双峰**——
  1~2cm（叠上了）和 5~6cm（滑到旁边，≈一个边长 4.68cm），3~4cm 谷底全场仅 5/1050。
  方块不可能半边悬空停住，4cm 阈值正落在空谷里。
- **11% 是判据误杀**：夹爪要求两指精确 0.0400、容差仅 ±0.104mm，实测这批停在
  0.0387（差 1.2mm）——**方块已松开**，只是 PD 没走完最后一毫米。已验证成功样本能
  到精确 0.04000，故属 episode 截断而非稳态误差。放宽到 3mm 可回收其中 71%，
  **生成产出约 +5pp**。（此问题不影响策略评测：评测有 400 步预算，夹爪来得及收敛。）
- **抓取偏心传导假设被否**：抓取偏心在成败两组只差 0.15~0.25cm，r=0.045/0.128，
  只解释放置误差方差的 0.2%~1.6%。
- **方块摆放位置完全不预测成败**：1050 样本上所有 |t|<2.1。排除了可达性解释。

##### 2.9.7 延长 horizon 无效（已验证并停止）

上轮所有失败都是 `horizon_exhausted`，故直接测。成功轨迹 p95 仅 299/311 步，需 >350
步才成功的仅占 2.1%/1.4%；horizon 放到 1200 重测，6 次成功全部在 211~242 步内完成，
2 次失败跑满 1200 步仍未完成。**失败是策略卡住，不是时间不够。**

##### 2.9.8 方法学结论（可能比原问题更有价值）

**用"每组训练一次 + 评测300次"这种标准做法，在这个任务上无法分辨约 7pp 以内的数据集
差异**——而绝大多数同类论文正是这么做的。证据链：
1. 未收敛训练会让单次训练内部就产生 22.5pp 波动
2. 即使训练收敛，换个训练种子仍有 ±3~4pp 波动
3. 种子平均能把表观效应从 −5.33pp 压到 −1.83pp

要得到可信结论必须**每组多个种子取平均**，单次训练的对比不可信。

##### 2.9.9 数据与脚本位置

- 评测结果 JSON（含每次 rollout 的成败/步数/耗时）：服务器
  `~/dev/mimic_enhance/eval_results_v2/`，v1 的备份在本地 `docs/pi05_eval_v1_results/`
- 训练 loss 曲线 `.npy`：同上目录
- 混合数据集（190干净+190arc，总量与 baseline 对齐以控制数据量混淆）：服务器
  `~/dev/mimic_enhance/lerobot_datasets/pi05_lerobot_mixed_half`，转换脚本多来源支持见
  commit `fed0937af`，转换后逐帧核对通过（state/action 完全一致、图像像素差 0）
- v1 的 99GB checkpoint 已删除（结论作废、且占满磁盘），证据文件已备份

#### 2.10 机制诊断：「接触段完全冻结」被实测推翻（2026-08-30）

**起因**：用户质疑——`sin²` 包络首末斜率为零、接触段冻结，接触就不该出错，成功率还是暴跌，
**大概率是判据对 arc 组不公平**。查下来：判据没问题（方向还反了），但用户的直觉指对了地方。

**方法上的收获（以后遇到扫描先试这个）**：v2 扫描的 per-run seed 让**不同幅度下的场景序列完全
相同**（500 个初始布局逐条匹配到 <1e-6 米）。于是可以取"同一场景、两个幅度都成功"的样本做配对
比较，**场景难度和 action noise 都成了共模、在配对差里抵消**。

**核心结论：冻结的是目标，不是状态。**

| arc 幅度 | 0.6 | 1.0 | 1.5 | 2.0 | 2.5 | 3.0 cm |
|---|---|---|---|---|---|---|
| 冻结段内实际末端偏差（三 seed） | 0.18 | 0.42 | 0.65 | 0.90 | 1.13 | **1.30~1.38 cm** |

约 **45% 的扰动幅度活着穿过「冻结」段**，比例在整条曲线和三个 seed 上稳定。配对放置误差同向：
同场景下 c1–c2 偏移从 −0.005cm 增到 **+0.385cm**，"arc 放得更差"的场景占比 49%→**72%**。

**三个支撑事实（全实测）**：
1. **机械臂永远落后目标约 5cm**：动作携带的位置误差中位数 7.40cm（扣掉每轴 3cm 的
   `action_noise` 后约 5.3cm），`scale=0.5` 后指令 3.70cm，实际单步只挪 **0.735cm**——只执行
   了指令的约 20%。3.7cm/步 在 20Hz 下是末端 74cm/s，物理上动不了那么快。**官方 MimicGen 也在
   这个状态下运行**，它没问题是因为每次都以同样的方式落后。
2. **绕弯让关节构型永久偏移**：resolved-rate DLS 逆运动学对关节速度积分，笛卡尔空间的闭合路径
   在关节空间不闭合。3.0cm 时关节差 **0.105 rad**，从倒数第 30 帧到最后一帧**完全不衰减**。
3. **残差是两个平衡点之差**：冻结段内两次运行指令完全相同，实测每帧只衰减 **0.76%、半衰期 90
   帧**，而冻结段只有约 18 帧。残差沿运动方向分量仅 37~41%（各向同性 33%）→ **不是滞后**。

**⚠️ 最要命的一条（用户 2026-08-31 指出，已核实 `data_generator.py:556-560`）**：
MimicGen 重放的是 `src_ep_datagen_info.target_eef_pose`——**源演示的目标位姿，不是它实际到达的
位姿**。源演示那条手臂自己也落后目标约 5cm，**复现这个落后量正是复现动作的方式**。
> **那 5cm 不是误差，是重放机制的一部分。任何"让手臂追上目标"的修法，都是把手臂推到源演示
> 从来没到过的地方。**

**判据是否对 arc 不公平：方向反了。** 归因于判据的失败模式（几何成立但夹爪没过）随幅度**持平或
下降**（三 seed：8.6→6.9%、11.5→7.6%、12.5→7.7%，绝对数也降）；新增失败全在抓取和放置上
（"cube_2 根本没被动过"每 500 次从 2 涨到 28）。而判据那个真 bug（空中判定，PR #7434 在修）
反而**对 arc 宽容**。

完整论证、脚本、逐 seed 日志：`docs/arc_sweep_diagnosis/`（README + REMEDIES.md）。

#### 2.11 五个修法的评判（2026-08-30/31，十二组生成实验各 300 次固定尝试）

用户提出三个方向，我另外从失败模式反推出两个。参照点复现了 v2 扫描（0.5cm 38.0% 对扫描 35.1%，
3.0cm 22.0% 对 19.1%）。

| 修法 | 3.0cm | 0.5cm | 差距 | 判定 |
|---|---|---|---|---|
| 无（参照） | 22.0% | 38.0% | 16.0pp | — |
| **方向一** 照抄平行组关节位姿 | 未实测 | — | — | **离线否掉**：分层后关节偏差 d 塌到 −0.65/+0.24，只是夹爪位置的代理；恢复它是"充分但不必要"，却最贵 |
| **方向二** 提高增益 scale 0.5→1.0 | 15.7% | **22.0%** | 6.3pp | **两端都崩**。差距缩小是把低幅度基准砸下去造成的假象。⚠️ 而且这**不是用户提的方向二**（见下） |
| **方向三** 驻留在 subtask 开头 | 24.7% | 42.3% | 17.6pp | 普遍抬升、差距没缩，**不治本**；episode 长 35%，单位时间产出反降 18% |
| 第四修法 只扰 subtask 0 | **33.3%** | — | — | 有效但**被"直接调小幅度"支配**（同成功率下注入扰动只有一半） |
| 第五修法 驻留在 subtask 结尾 | 19.7% | 35.0% | 15.3pp | **收敛做到最好（p05 从 2.50cm→0.46cm）成功率却不动** |

**最有信息量的是第五修法**：我们成功让手臂在接触时刻收敛到目标，成功率没变好——结合 2.10 那条
"目标本身就该被落后 5cm"，解释就通了：收敛到目标 = 推离源演示走过的位置。

**失败模式揭示损伤是局部的，不是累积传导的**：超额失败数 k=1→14、k=2→29、k=4→48，**线性于 k**
（随机游走 √k 预测 14/20/28，差得远）。只扰 subtask 0 时放红块失败回到基线（115 对 114）。
残差确实在累积（0.63→1.28cm 实测），但**累积的残差不是失败原因**——我把相关当成了因果，已订正。

**载体是什么：三个候选全部排除，未知。** 关节构型（分层否掉）、接触时手臂位置（第五修法否掉）、
方块在夹爪里的偏心（d=0.22~0.32、相关 0.02~0.18）。**如实标为未解。**

**用户提的方向二（正确版本）仍在测**：不是提高增益去追那个够不着的目标，而是**跑一组平行
MimicGen、用它实际到达的夹爪位置当参照去靠拢**。实现：按场景布局查表 + 冻结段斜坡重定向 +
无噪声驻留。第一次跑**无效**（见 2.13 教训），已修正重跑。

#### 2.12 两个 MimicGen upstream bug（PR #7433 / #7434，2026-08-29）

**#7433 `max_num_failures` 从未被读取**：`git grep -c` 只有 1 处定义 + 18 处赋值、**零处读取**，
从 `5cf3d6185`（#179 Mimic 初次引入）起就是死的。真实终止只看成功数，低成功率会无界重试。
闭环验证（upstream 原版任务、cap=25）：原版跑到 **50 次失败仍不停**；接上字段后**精确停在 25**。
⚠️ 修法**不能直接按文档实现**——18 个 shipped config 都设了 25，直接生效会让
`--generation_num_trials 1000` 在约 40 次尝试后中止。改成默认 `None` 不设限 + 移除那 18 处失效
赋值，现有行为完全不变。

**#7434 `cubes_stacked` 在方块自由落体途中判成功**：判据只验瞬时几何、不要求方块静止。实测两个
交付数据集：baseline **28/380（7.4%）**、arc **19/358（5.3%）** 的"成功"demo 末帧方块是散的
（cube_3 落在横向 6.3cm 外、与 cube_2 同高 = 桌面上）。判别量：判据成立帧的 cube_3 速度中位数
0.028~0.032 m/s，正常的是 **0.0000**。闭环：缺陷率 **9.0% → 2.0%**，成功率代价 2.5pp。
阈值 `max_lin_vel=0.05` 是量出来的：静止方块因接触求解器抖动读数中位 0.0132、最大 0.0341 m/s，
空中方块中位 0.104。⚠️ **不是完美分离器**（弹跳顶点会短暂变慢），诚实写进了 PR。

**⚠️ 一条已作废的旧记录**：CLAUDE.md 曾记"11% 是夹爪判据误杀，放宽到 3mm 可回收 71%、产出
+5pp"。**这条是错的**，照它改会把 54 条"摔方块"的 episode 当成成功演示写进训练数据。那 76 个
episode 全是掉落：末帧几何成立 0/76，方块平均移动 10.8cm。真正的缺陷方向相反（#7434）。

**PR 材料**：正文、复核脚本、四组闭环日志在 fork 的 `mimic-pr-evidence` 分支
`docs/mimic_pr_evidence/`；两个 PR 分支 `fix/mimic-max-num-failures`、`fix/cubes-stacked-at-rest`。

#### 2.13 方向二（靠拢平行组实际位姿）：首次有效实验，结果为负（2026-09-02）

用户提的方向二不是"提高增益去追目标"（那是我误测的版本，已否），而是**跑一组平行 MimicGen、
用它实际到达的夹爪位置当参照去靠拢**。实现：按场景布局查参照表 + 冻结段斜坡重定向 + 无噪声驻留。

**前三次运行全部无效**（见坑速查"静默失效"条）。第四次经机制核验后生效：查表命中 92~93%、
episode 长度 239→319。

Group D（`num_envs=10`+重新播种，**只能组内比**，见 `EXPERIMENT_LEDGER.md`）：

| 组 | 干预 | 成功率 |
|---|---|---|
| `d2b_ref` | arc 0.5cm | 32.3% |
| `d2b_arc` | arc 3.0cm，无干预（对照） | **15.7%** |
| `f_snap_hold` | 靠拢 + 驻留 | **12.0%** |
| `f_snap_only` | 靠拢，不驻留 | **13.0%** |
| `f_hold_only` | 只驻留（瞄准原目标） | 17.3% |

**⚠️ 但这不是对方向二的公平判决——我在实现里犯了和 2.10 同一类的错。** 验证"靠拢是否真的
让夹爪贴近参照位姿"，结果是**反的**（到参照组接触位姿的距离）：

| 组 | 接触1 | 接触2 | 接触3 | 接触4 |
|---|---|---|---|---|
| **对照（什么都不做）** | **0.69cm** | 0.90cm | 0.99cm | 3.79cm |
| 靠拢+驻留 | 1.59cm | 1.97cm | 3.16cm | 12.18cm |

原因：我把**目标**设成了参照组的**实际位姿**，而手臂落后目标约 5cm，于是停在参照位姿再往后
5cm 处。**而什么都不做的对照，追的是和参照组同一个目标、以同样方式落后，自然就落在参照组
实际到过的位置附近。** 我"修正"了一个本来已经对的东西。

**所以方向二仍未被公平测试。** 正确实现要补的是"两次运行滞后量之差"（逐步反馈量），
而不是把目标直接设成对方的实际位姿。

**但这一轮给了一条强旁证**：对照组在第一次接触时**已经只差参照组 0.69cm**（方块 4.68cm、
夹爪每边余量 1.66cm），而成功率仍从 32.3% 掉到 15.7%。**接触位姿基本一致、成功率仍然腰斩**
——比 2.11 那条"载体不是接触时手臂位置"的间接推断强得多，这次是直接量出来的。

用户的物理论证（位姿一样就不该失败）**严格说仍未被证伪**，因为 0.69cm ≠ 0；但要用它解释
16.6pp 的成功率差，需要说明 0.69cm 如何在 1.66cm 余量下造成这么大影响。

#### 2.14 接触帧驻留：载体找到了，2.10/2.11/2.13 的三条结论撤回（2026-09-02/03）

**一句话**：在每次夹爪指令前插 20 帧无噪声驻留、停在序列里现成的名义目标上，arc 3.0cm 生成
成功率 15.7% → **39.0%**（超过 0.5cm 不驻留的 32.3%），0.5cm 参照自己 → **50.3%**。不需要
平行组。台账 Group E（`docs/arc_sweep_diagnosis/EXPERIMENT_LEDGER.md`），脚本
`contact_hold_trial.py` 等，输出 `datasets/arc_sweep_diagnosis_runs/contact_hold/`。

| 组 | arc | 干预 | 成功率 | 闭合帧到方块 中位/p90 | 释放帧方块偏移 中位/p90 |
|---|---|---|---|---|---|
| ref_none | 0.5cm | 无 | 32.3% | 1.12 / 2.08 cm | 1.37 / 4.68 |
| arc_none | 3.0cm | 无 | 15.7% | 1.42 / 3.07 | 2.01 / 9.54 |
| arc_snap | 3.0cm | 抄平行组接触位姿 + 驻留 | 19.7% | 1.18 / 2.59 | 1.47 / 7.85 |
| arc_hold | 3.0cm | 名义目标驻留 | **39.0%** | 0.85 / 1.99 | 1.17 / 5.34 |
| ref_hold | 0.5cm | 名义目标驻留 | **50.3%** | 0.84 / 1.66 | 0.92 / 1.70 |

源演示（人）：0.56 / 0.90cm。两个对照组跟 Group D 逐位一致，驻留组 episode 230 → 310 帧。

**撤回的三条**：
- ⚠️ 2.11"载体三候选全排除、接触时手臂位置不是载体"——**撤回**。第三/第五修法的驻留分别在
  下一 subtask 开头和 subtask 末尾，都在夹爪动完 11~23 帧之后（`grasp_1`/`grasp_2` 信号跟夹爪
  动作同帧触发，`datagen_info_pool.py:157-163` 信号后 1 帧结束，再加 offset 10~20），从没在
  接触帧收敛过；0.69cm 是成功样本中位数，失败样本 1.88cm。载体就是接触帧的水平位置。
- ⚠️ 2.10"那 5cm 不是误差是重放机制的一部分，任何追上目标的修法都是推到源演示没到过的地方"
  ——**对运动过程成立，对接触帧不成立**。源演示夹爪动作帧目标−实际中位 0.00cm（70% <0.3cm），
  普通帧 4.86cm。人松开摇杆、等臂停稳、再动夹爪；那一帧目标 = 实际。
- ⚠️ 2.13"方向二未公平测试，原因是滞后"——原因主要是**修正量错了时刻**：`参照接触位置 − subtask
  最后一帧目标`，而最后一帧在接触后 11~23 帧，源演示手臂那段走 10~17cm。这次修正后到参照距离
  0.69 → 0.36cm，是公平测试，结果 19.7%：平行组闭合时离方块 1.12cm、自己只成功 32%，抄它
  = 抄它的噪声误差和失败位姿（同场景配对：参照成功的场景 27.5 → 47.9，失败的 3.5 → 6.2）。

**分阶段漏斗**：驻留作用在放置（51.7 → 69.7%）和第二次抓取（条件成功 75 → 91%），第一次抓取
本来就 95~99%。**MimicGen 自己 62% 的失败大头是同一机制**：目标序列在源演示帧号停住，生成的
手臂落后更多，夹爪指令下达时还在以 1.7cm/5 帧赶路（人 1.14、驻留后 0.64）。

**patch 线索（用户 9/3 定）**：官方 `SubTaskConfig.num_fixed_steps` 就是驻留，但放在 subtask
**开头**、所有 Franka stack 配置设 0。该加的是"夹爪动作前驻留/按收敛门控夹爪"。后续开发。

**剩下的 11.3pp**（39.0 对 50.3）几乎全在"方块2 放到方块1 上"（61.3 对 69.7）：释放帧方块偏移
p90 5.34 对 1.70，中位已对齐——手臂到位了、方块在夹爪里歪了，指向沿 arc 路径接近时的抓取方式。
下一个要量的。**局限**：一个生成种子、n=300、驻留 20 帧未调、episode 长 35%、训练侧影响未测。

**用户定的方向（9/3）**：把"停在源演示实际位姿"和"抓取阶段目标点驻留"结合，追求高产出率
（接触帧上两者是同一点，可探索的是驻留长度、按收敛门控、作用事件、多种子）。

#### 2.15 参数与适用范围：换场景、冻结比例、门控、生产工作点（2026-09-03，台账 Group F）

| 条件 | 成功率 | 释放帧方块偏移 中位/p90 |
|---|---|---|
| MimicGen 0.5cm，不驻留 | 32.3% | 1.37 / 4.68 cm |
| + 固定驻留 20 帧 | 50.3%（换一串场景 55.3%） | 0.92 / 1.70 |
| + **按收敛门控驻留**（0.3cm / ≤40 帧） | **55.0%** | 0.85 / 1.57 |
| arc 3.0cm 冻结 30%，不驻留 | 15.7% | 2.01 / 9.54 |
| + 固定驻留 20 帧 | 39.0%（换场景 40.3%） | 1.17 / 5.34 |
| + 门控驻留 | 44.0% | 1.15 / 6.98 |
| arc 3.0cm **冻结 50%** + 固定驻留 | **49.7%** | 0.93 / 1.73 |
| arc **1.2cm**（生产工作点）冻结 30% + 固定驻留 | **50.0%** | 0.91 / 1.78 |

读法：①驻留效果在第二串场景上复现；②门控比固定 20 帧再高约 5pp、平均帧数相当（17.8/19.5），
但五分之一的驻留到 40 帧上限仍没进 0.3cm（大概率在释放时目标被下方方块顶住），阈值没扫过；
③arc 残余损失是接近段造成的——冻结从 30% 加到 50%，3.0cm 跟不加 arc 持平、释放尾巴消失，代价是
同一峰值下路径积分扰动少约三成；门控单独不消这条尾巴；④1.2cm 工作点驻留后 50.0%，对比 v1 生产
时的 34.1%。

**当前证据下的推荐组合**：自由段 arc（峰值到 3.0cm 用冻结 50%，1.2cm 用冻结 30%）+ 每次夹爪
动作前按收敛门控的无噪声驻留（0.3cm / ≤40 帧）。预期产出 50~55%，原版 MimicGen 32%。**没跑的**：
门控 + 冻结 50% 同用、门控在 1.2cm、阈值/上限扫描、门控换场景。局限同 2.14。

**代码位置**：`docs/arc_sweep_diagnosis/contact_hold_trial.py`（`CONTACT_FIX=none|hold_target|
snap_ref|gate_target`，`HOLD`/`GATE_TOL`/`GATE_MAX`/`RESEED_BASE`），三个 runner 脚本，
`contact_hold_analysis.py` 出机制核验表。都是猴子补丁，没改安装的代码；做正式 patch 的位置见
2.14 末尾。

#### 2.16 加大幅度：手臂只走出指令弧线的四成，代价在搬运段（2026-09-04，台账 Group G）

用户提出既然接触帧驻留能拉回来，就把幅度推到 10cm 让轨迹差异明显超出噪声。先离线算几何
（`big_arc_geometry.py`）：包络峰值处末端离桌面 16~21cm，10cm 往下也碰不到桌面；各向同性方向蹭到无关
方块 3~4%。于是跑 0/1.2/3/5/8/10cm × 门控驻留（上限 60）× 冻结 30%，并把每段**指令路径**存下来
（`CMD_DIR`），用 `effective_amplitude.py` 量手臂**实际**走出的弧线：

| 名义 | 指令峰值 | 实际峰值 中位/p90 | 实际/指令 | 方块2 放上 | 成功率 |
|---|---|---|---|---|---|
| 0 | 0 | 0 | — | 71.1% | 51.5% |
| 1.2cm | 1.16 | 0.59 / 1.03 | 0.51 | 69.3% | 51.7% |
| 3.0cm | 2.79 | 1.24 / 2.36 | 0.45 | 61.3% | 41.0% |
| 5cm | 4.35 | 1.88 / 3.52 | 0.43 | 46.0% | 26.0% |
| 8cm | 6.87 | 2.86 / 5.32 | 0.42 | 27.0% | 9.7% |
| 10cm | 8.34 | 3.49 / 6.62 | 0.42 | 16.3% | 6.0% |

**原因（代码 + 数据，`big_arc_cause.py`）**：动作 = 目标 − 当前 + 噪声，乘 `scale=0.5`，DLS IK 转成
关节目标，PD（刚度 400/阻尼 80）在 50ms 里只实现一部分——实测每帧沿误差方向走约 0.11×误差（<4cm），
再大则次线性（0.27/0.33/0.40 cm/帧）。**整条控制链是时间常数约 10 帧的一阶跟踪器**，四十帧内起落的
鼓包会被削到四五成，跟幅度无关；5cm 以上还撞上手臂实际速度范围（p90 1.5cm/帧，与源演示相同）。
关节限位和关节速度限位都**不是**原因（数据里都不贴边）。成功率掉在**搬运段**：第一次抓取 99.7→87.7%，
"方块2 放到方块1 上" 71→16%，释放时方块在夹爪里的偏心 0.86→2.84cm（10cm 时 p90 13.5cm——方块已
飞出）。横向加速度让方块在指间滑动，这发生在任何驻留之前，驻留时间再长也救不回。

⚠️ 之前口头一直把 `PERTURB_ARC_STD` 当"扰动幅度"——实际手臂走出来的只有它的四到五成，生产用的
1.2cm 实际是 0.59cm。以后报幅度报**实际**值。

**拉长测了（台账 Group H，`PERTURB_ARC_STRETCH=2`）：不行。** 0cm 拉长 52.7%（对照干净）；5cm 拉长
22.3%（不拉长 26.0%），10cm 拉长 4.3%（6.0%）。手臂确实多追上了鼓包（实际/指令 0.38 → 0.59，符合
10 帧一阶模型对 80 帧鼓包的预测），**但追上了反而放置更差**：进冻结段时离标称路径更远（5cm：1.9 → 2.4cm），
18 帧尾巴 + 门控拉不回来（接触残差 0.64 → 1.03cm），方块偏心 1.20 → 1.75cm。方向依赖：不是奇异姿态
（径向/切向 0.33~0.43 差别小），竖直鼓包跟得最差（0.25~0.28 对水平 0.44~0.45）。**结论：3cm 以上的
限制不是"跟不上"，是"跟上了回不来 + 搬运时方块被甩歪"。** 大幅度的损失全在搬运段（subtask 1、3）。

**没测的**：①只在接近段（subtask 0、2，夹爪空）加 5~10cm 弧线（Group A 在 3cm 无驻留下 `ARC_SUBTASKS=0`
是 33.3% 对 22.0%）；②只用水平方向；③大幅度配更长冻结尾巴。

**原因指向的解法（原记录，①已测否）**：①把自由段在时间上拉长（重采样指令段，让鼓包持续几个时间常数）——
鼓包是太快不是太大；②在门控保护接触的前提下重试提高 `scale`（Group A 那次没有驻留）；③搬运段
（subtask 1、3）用小幅度。方向先验（上半球/人类锥）也没测。

## 主线二的成功判据（2026-08-21 用户定义，两层，别混着说）

**第一层（生成阶段的门槛，现在这轮扫描测的就是这个）**：加了位姿扰动后，
在某个σ区间内生成成功率跟原版 MimicGen（不加扰动）差不多——说明加扰动没把数据搞坏
（留下来的每条演示仍然真实通过任务成功判据），能拿到"够多、能用"的数据。
**这一层只是前置门槛，不是最终结论**——它只能证明"没亏"，不能证明"更好"。

**第二层（真正验证假设的地方，还没跑）**：把这批数据喂给 LeRobot 训 DP/ACT，
再回仿真里测评，看成功率/鲁棒性是不是真的比"不加扰动的原版 MimicGen 数据"更好。
**"扰动增广的数据基本质量更高、更适合训练"这句话，要等第二层跑完才能下结论，
现在这轮σ扫描曲线不能替代它。** 中途任何报告都不该把第一层的结果说成第二层的结论。

**曲线出来后的下一步**：从曲线里挑一个"生成成功率接近基线"的 σ 作为正式操作点，
用它生成够量的训练数据——如果挑中的σ在扫描里成功率还不错（比如~30%+这个量级），
**扫描本身产出的成功demo可能就够直接用**（500次×30%×5seed≈750条量级），
不一定要再单独跑一轮"凑够N个成功"模式；具体够不够，等曲线出来看数字再定。

⚠️ **这条主线的执行方式（用户当场定的规矩）**：即使是 goat 模式/自动化运行，
每步源码/脚本改动仍必须单独 `git commit`，不许因为无人盯着就放宽或攒批提交。

## 还会复发的坑速查（跨主线通用，不是项目叙事——遇到同类问题先看这里）

- **`run_in_background` Bash 任务长跑会被莫名杀掉**（约61分钟观察到过，非OOM/非磁盘满/
  查不到dmesg记录，可能专门针对Claude Code自己track的这类任务，机制未查实）。
  **解法**：`setsid nohup ... </dev/null & disown` 让进程树在OS层面彻底脱离session
  （`ps`查`SID==PID`验证），改用自建`Monitor`盯日志+`pgrep`心跳，不依赖Claude Code
  对该任务的存活追踪。
- **父进程被杀后，子进程可能真的卡死**（吃CPU但输出不再增长）——如果用了
  `subprocess.run(..., capture_output=True)`，父进程死后PIPE读端消失，子进程写日志时
  可能卡在处理不了的SIGPIPE/EPIPE上。**解法**：别用`capture_output=True`，子进程
  stdout/stderr直接重定向到打开的日志文件。
- **提高并行度（如GPU渲染的`num_envs`）不一定稳**——测过更高并行度，显存到92%时前面
  更快，但后面会卡死零进展（根因未查清）。**别为了吞吐盲目冲更高并行度**，稳定性优先，
  用已经反复验证过的配置。
- **配置字段的文档字符串描述的是设计意图，不是实际生效的行为**——务必去读真正执行
  判断的代码（`max_num_failures`那次教训：字段18处赋值但代码里从未被读取）。
- **视觉/多任务变体可能是独立的env cfg类，不会自动继承其它变体的环境变量接线**——
  换任务变体（比如从纯状态换成带摄像头）时，先确认目标参数是不是真的在新类里生效，
  不要想当然认为"同一套环境变量到哪个task都该管用"。
- **训练超参配错可以完全静默，而且会伪造出"显著"的实验结论**——2.9.3 那次
  `peak_lr == decay_lr` 让余弦退火退化成恒定学习率，openpi 日志里打了
  `this results in a constant schedule` 但没人看。模型全程不收敛，**同一次训练内相隔
  4000 步的两个 checkpoint 闭环成功率差 22.5pp**，把它当结论就得到一个 p=0.001 的假
  发现。**解法**：每次训练完必查两个量——① `param_norm` 分段增量是否逐段减速（不减速
  = 没收敛）；② 末段 loss 斜率的 t 值是否已 |t|<2。
- **任何"A组比B组好X个百分点"的结论，先测噪声底线再说**——同一批数据只换训练种子重训
  一次，实测就能差 ±3~4pp。没有这把尺子，单次训练的组间对比不可信。种子平均能把表观
  效应从 −5.33pp 压到 −1.83pp。
- **报告"任务在跑"之前必须核实它真的在推进，不能只看启动成功**——踩过两次：一次把
  OOM 崩溃 18 秒的训练当成"已跑 4 小时"；一次用错误的日志格式 grep 导致误判"训练卡死"
  （实际正常）。**解法**：查进度计数是否增长 + GPU 利用率 + 进程 CPU 时间，三者交叉。

- **静默失效的干预会伪装成"干净的负结果"**——这一轮踩了三次。①追加的驻留帧继承了
  `action_noise=0.03`（每轴 3cm），手臂在原地被噪声推着走，看起来像"驻留无效"；②按场景查表
  容差卡到 0.1mm，56% 查不到、修正分支静默不执行，看起来像"靠拢无效"；③对照组和修正组给出
  **完全相同的 47/300**——真改了轨迹不可能一条不差。**解法：每个干预都要有一个"它确实发生了"
  的机制指标**（episode 是否变长、收敛指标是否改善、查表命中率），光看成功率必然被骗。
- **同一批实验里换了随机化方式，绝对数值就不可跨轮比较**——加了确定性重新播种后，两个参照点
  双双下移（0.5cm：38.0%→32.3%；3.0cm：22.0%→15.7%）。**播种方式本身影响成功率**，只能在同
  一轮内部比。
- **并行环境数可能本身影响成功率**：`num_envs=1` 得到 40.7%/28.0%，`num_envs=10` 得到
  38.0%/22.0%。还没定量核实，但引用扫描绝对值时要留意这个自由度。
- **配对分析前先验证"能不能配对"**——`action_noise` 每步抽一次全局随机数，两次运行 episode
  长度不同就会让后面的场景 RNG 错位。实测 `num_envs=10` 时 150 条只匹配上 8 条，`num_envs=1`
  也一样。修法是**每个 episode reset 前用确定性种子重新播种**；但注意重新播种只能把场景复现到
  约 0.2mm（中位）/1.4mm（p90），**匹配容差要按实测的判别间隙定**（正确匹配 ≤1.4mm vs 最近的
  不同场景 ≥23mm，取 2mm 安全）。参照表还必须和目标运行用**相同的 `num_envs`**——启动时那次
  reset 一次性给所有环境抽场景，1 环境和 10 环境从第一条就分叉（命中率 75% vs 5%）。
- **两个排队脚本不能等同一个"空闲"条件**——都等"没有 X 在跑"，第一批结束的瞬间双双判定为真、
  同时启动，两个 Isaac Sim 抢一张 12G 卡。**要等对方的完成标记。**
- **重放型流水线里，"误差"可能是机制的一部分**——MimicGen 重放的是源演示的**目标位姿**而不是
  实际位姿，源演示的手臂本来就落后目标约 5cm，复现这个落后量才复现出动作。任何"消除跟踪误差"
  的优化都会把手臂推离源演示走过的路。**改一个量之前先问它是不是被依赖的。**

- **干预对准的是"事件"还是"subtask 边界"，先量两者差几帧**——三次驻留实验（第三、第五修法、
  方向二第一版）没有一次落在夹爪动作那一帧上，边界在夹爪动完 11~23 帧之后，源演示手臂那段走
  10~17cm。修正向量、驻留位置都要锚在事件帧。
- **一个量的全局中位数不是它在关键帧上的值**——"目标领先实际 5cm"是全帧中位，夹爪动作帧上是
  0.00cm，据此推出的"追上目标就是推离源演示"在关键帧上反了。关键帧单独量。

## 改动纪律

- **源码/功能改动（`.py`/`.sh`/`.yaml`/`.json` 等）→ 改完就 `git commit`**，
  提交信息写清"改了什么、为什么"。
- **纯文档/注释改动（如 `CLAUDE.md`）→ 不必每次都提交**。
- **不用"复制到 backups/ 目录"当改动前的保护**——备份靠人肉比对容易出错、越堆越乱，
  且用户没法拿它 `git diff`。改动前该做的是确认工作区干净、需要时用 git 本身的机制
  （stash/commit/diff），git 记录精确到行，比手工备份可靠。

## 取证与验证纪律

前提：结论必须能落到**用户自己能核对的数字，或者自己能跑的命令**上，
不能停留在"听起来对"的层面。

**① 精确引用/需要写进正式产物的原文，用 `curl -o` + `grep`，不用 `WebFetch` 转述。**
```bash
curl -sfL -o <文件> <raw链接>      # -f: 404 时失败，避免把错误页当源码存下来
grep -qF "<要核对的原文>" <文件>    # -F: 当纯文本匹配，避免代码里的括号/点被当正则
```
`WebFetch` 拿到的不是原文，是"抓取 + 小模型转述"，用于精确引用会走样。
⚠️ 用 `-o` 写盘、不进上下文；裸 `curl`/`cat` 大文件才是上下文暴涨的真凶。
渲染页面或只需要"确认存在"这类弱需求，用 `WebFetch` 没问题；
需要精确原文（尤其是要引用到公开产物里）时才必须换成 curl+grep。

**② 验证优先级：能推算的推算 → 能跑测试的测 → 实在不行才做重量级的实际运行/仿真**
（这类验证通常耗时且成本高，作为最后手段）。
⚠️ 最需要警惕的情况：既没有可验证的数字、也没有可运行的验证手段——
这种结论必须明确标成"未验证的推测"，不能当作已确认的结论使用。

**③ 自查话术红灯**：出现"逻辑上应该没问题"/"参考了官方实现"/"这样改更合理"这类
缺乏实测支撑的表述时，应该先自问（或反问用户）**"这个改动在什么情况下会出错？"**，
把结论落实到具体场景，而不是停在直觉判断上。

**④ 子 agent 的定位是"唱反调的第三方视角"，不是事实核查工具。**
子 agent 和主 agent 容易共享同类系统的失效模式，"另一个 AI 说没问题"不构成证据；
用它来提供独立视角/挑刺，而不是用它的结论替代实测。

## 沟通风格偏好

- **默认使用中文交流**（代码、注释、变量名等技术产物仍用英文/项目原有语言，
  沟通说明用中文）。
- **做判断、排查、取舍时，展示推理过程而不是只甩结论**：讲清"考虑了哪些选项、
  为什么选这个、什么情况下会不对"，让用户能在过程中发现问题及时纠正，
  而不是等到最终结论或代码改完才能审查。
- **边界**：纯执行性操作（读文件、跑已经谈妥的命令、按已定方案编辑代码）
  不需要展开讲；只在真正存在判断/取舍/不确定性的节点才展开推理过程——
  否则会退化成流水账，反而埋没真正的决策点。
