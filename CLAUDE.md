# IsaacLab 项目工作准则（两条主线共同部分）

> 个人通用规矩在 `~/.claude/CLAUDE.md`（每个 session 自动加载），这里只放**项目事实**和**跨主线规则**；
> 本主线的状态由文末的 `@` 导入加载，另一条主线的内容不在本分支的上下文里。

## 项目目标与布局

用 **Isaac Sim + Isaac Lab** 操纵**两台 Franka 机械臂**做**家具装配**：遥操作采集少量演示 → MimicGen 放大
→ 策略训练。两条主线并行，代码基线不同，**永远不共用检出**：

| 主线 | 分支 | 目录 | 基线 | 一句话 |
|---|---|---|---|---|
| 一：家具装配 / VR 遥操作 / 基准调研 | `mainline1-furniture` | `~/IsaacLab-ml1` | Isaac Lab 3.0 | VR 遥操作已在实验室服务器打通；资产与基准调研进行中 |
| 二：接触锚定的弧线扰动（arc） | `mainline2-arc` | `~/IsaacLab-ml2` | Isaac Lab 2.3.2 | 接触帧驻留 + 峰值提前，5~10cm arc 产出 47~51% |
| 上游 PR（`fix/*`） | 各自分支 | 主检出 `/home/pk/IsaacLab` | develop | #7380 在审，#7381 已合，#7433/#7434 等审 |

- 数据不进 git：`datasets/`（gitignore）按主线分子目录，路径记在各主线台账里。
- 同一主线并行工作开短命任务分支（`ml2/<task>`），只有主线 session 合并，并回即删。
- 备份/云端：fork `2047767028-lang/IsaacLab`；推送等用户说。

## 环境事实

- 本机：conda `isaaclab`（2.3.2，`/home/pk/miniconda3/envs/isaaclab/bin/python`），RTX 4080 12GB；
  `conda run` 不传 stdin，脚本写文件再跑。conda `lerobot_v21` 有 lerobot 0.3.3。
- 实验室服务器：`surf2@192.168.3.254`，只有有线口 `enp109s0` 在线时可达，本机 TUN 代理会拦截，用
  `ssh -b 192.168.3.123`；GPU 共享（4×5090），`~/dev/mimic_enhance/` 无版本控制。
- **本机怎么跑 develop 分支（3.x）的测试**：本机 conda `isaaclab` 是 2.3.2，`import isaaclab`
就要 Kit（`utils/mesh.py` 导 `pxr`），而且本地根本没有 `contrib/stack`、`isaaclab_teleop`。
办法：给 PR 分支建 worktree；`pip install --target <临时目录> lazy_loader`（develop 新依赖，
把顺带装进来的 `packaging*` 删掉，否则会压掉环境里的版本）；`PYTHONPATH` 放 worktree 下
**全部** `source/*` 包外加那个临时目录，然后
`conda run -n isaaclab python -m pytest <文件> -p no:cacheprovider`。只放 `isaaclab`/
`isaaclab_tasks` 不够——`isaaclab_assets` 会落到本机 2.3.2 版本，报
"`RigidBodyPropertiesCfg` has moved to `isaaclab_physx`"。develop 核心包已是懒加载，纯张量
逻辑的单测 1 秒内导入完、不加载任何 `omni`/`carb`/`pxr`。需要 `carb`/`pxr`/`isaacteleop` 或
真实 teleop 进程的测试文件本机跑不了，是环境限制；判断有没有回归要拿原封不动的
`origin/develop` 用同样方式跑一遍做对照，比对每个文件的通过数。develop 的 lint 是
**ruff 0.14.10 + codespell**（不是 black/isort），在 worktree 根目录跑
`uvx ruff@0.14.10 check/format --check <文件>` 就能吃到 pyproject 里的配置。

## 改动纪律

- **多 session 并行时，只在自己的 worktree 分支上工作**（`.claude/worktrees/<name>`），每步改动一个
  commit，**不推送、等用户说**；不碰别的 worktree 和主检出。主检出 `perturbation-experiment` 的
  CLAUDE.md 停在 9/2，9/2~9/4 的内容只在 `mainline2-arc` 分支上。

- **源码/功能改动（`.py`/`.sh`/`.yaml`/`.json` 等）→ 改完就 `git commit`**，
  提交信息写清"改了什么、为什么"。
- **纯文档/注释改动（如 `CLAUDE.md`）→ 不必每次都提交**。
- **不用"复制到 backups/ 目录"当改动前的保护**——备份靠人肉比对容易出错、越堆越乱，
  且用户没法拿它 `git diff`。改动前该做的是确认工作区干净、需要时用 git 本身的机制
  （stash/commit/diff），git 记录精确到行，比手工备份可靠。
- 多 session：只在自己的 worktree 分支上工作；主检出只用于 `fix/*` PR 分支。

## 还会复发的坑速查（跨主线通用，不是项目叙事——遇到同类问题先看这里）

- **`run_in_background` Bash 任务长跑会被莫名杀掉**（约61分钟观察到过，非OOM/非磁盘满/
  查不到dmesg记录，可能专门针对Claude Code自己track的这类任务，机制未查实）。
  **解法**：`setsid nohup ... </dev/null & disown` 让进程树在OS层面彻底脱离session
  （`ps`查`SID==PID`验证），改用自建`Monitor`盯日志+`pgrep`心跳，不依赖Claude Code
  对该任务的存活追踪。
- **父进程被杀后，子进程可能真的卡死**（吃CPU但输出不再增长）——如果用了
  `subprocess.run(..., capture_output=True)`，父进程死后PIPE读端消失，子进程写日志时
  可能卡在处理不了的SIGPIPE/EPIPE上。**解法**：别用`capture_output=True`，子进程
  stdout/stderr直接重定向到打开的日志文件。
- **提高并行度（如GPU渲染的`num_envs`）不一定稳**——测过更高并行度，显存到92%时前面
  更快，但后面会卡死零进展（根因未查清）。**别为了吞吐盲目冲更高并行度**，稳定性优先，
  用已经反复验证过的配置。
- **配置字段的文档字符串描述的是设计意图，不是实际生效的行为**——务必去读真正执行
  判断的代码（`max_num_failures`那次教训：字段18处赋值但代码里从未被读取）。
- **视觉/多任务变体可能是独立的env cfg类，不会自动继承其它变体的环境变量接线**——
  换任务变体（比如从纯状态换成带摄像头）时，先确认目标参数是不是真的在新类里生效，
  不要想当然认为"同一套环境变量到哪个task都该管用"。
- **训练超参配错可以完全静默，而且会伪造出"显著"的实验结论**——2.9.3 那次
  `peak_lr == decay_lr` 让余弦退火退化成恒定学习率，openpi 日志里打了
  `this results in a constant schedule` 但没人看。模型全程不收敛，**同一次训练内相隔
  4000 步的两个 checkpoint 闭环成功率差 22.5pp**，把它当结论就得到一个 p=0.001 的假
  发现。**解法**：每次训练完必查两个量——① `param_norm` 分段增量是否逐段减速（不减速
  = 没收敛）；② 末段 loss 斜率的 t 值是否已 |t|<2。
- **任何"A组比B组好X个百分点"的结论，先测噪声底线再说**——同一批数据只换训练种子重训
  一次，实测就能差 ±3~4pp。没有这把尺子，单次训练的组间对比不可信。种子平均能把表观
  效应从 −5.33pp 压到 −1.83pp。
- **报告"任务在跑"之前必须核实它真的在推进，不能只看启动成功**——踩过两次：一次把
  OOM 崩溃 18 秒的训练当成"已跑 4 小时"；一次用错误的日志格式 grep 导致误判"训练卡死"
  （实际正常）。**解法**：查进度计数是否增长 + GPU 利用率 + 进程 CPU 时间，三者交叉。

- **静默失效的干预会伪装成"干净的负结果"**——这一轮踩了三次。①追加的驻留帧继承了
  `action_noise=0.03`（每轴 3cm），手臂在原地被噪声推着走，看起来像"驻留无效"；②按场景查表
  容差卡到 0.1mm，56% 查不到、修正分支静默不执行，看起来像"靠拢无效"；③对照组和修正组给出
  **完全相同的 47/300**——真改了轨迹不可能一条不差。**解法：每个干预都要有一个"它确实发生了"
  的机制指标**（episode 是否变长、收敛指标是否改善、查表命中率），光看成功率必然被骗。
- **同一批实验里换了随机化方式，绝对数值就不可跨轮比较**——加了确定性重新播种后，两个参照点
  双双下移（0.5cm：38.0%→32.3%；3.0cm：22.0%→15.7%）。**播种方式本身影响成功率**，只能在同
  一轮内部比。
- **并行环境数可能本身影响成功率**：`num_envs=1` 得到 40.7%/28.0%，`num_envs=10` 得到
  38.0%/22.0%。还没定量核实，但引用扫描绝对值时要留意这个自由度。
- **配对分析前先验证"能不能配对"**——`action_noise` 每步抽一次全局随机数，两次运行 episode
  长度不同就会让后面的场景 RNG 错位。实测 `num_envs=10` 时 150 条只匹配上 8 条，`num_envs=1`
  也一样。修法是**每个 episode reset 前用确定性种子重新播种**；但注意重新播种只能把场景复现到
  约 0.2mm（中位）/1.4mm（p90），**匹配容差要按实测的判别间隙定**（正确匹配 ≤1.4mm vs 最近的
  不同场景 ≥23mm，取 2mm 安全）。参照表还必须和目标运行用**相同的 `num_envs`**——启动时那次
  reset 一次性给所有环境抽场景，1 环境和 10 环境从第一条就分叉（命中率 75% vs 5%）。
- **两个排队脚本不能等同一个"空闲"条件**——都等"没有 X 在跑"，第一批结束的瞬间双双判定为真、
  同时启动，两个 Isaac Sim 抢一张 12G 卡。**要等对方的完成标记。**
- **重放型流水线里，"误差"可能是机制的一部分**——MimicGen 重放的是源演示的**目标位姿**而不是
  实际位姿，源演示的手臂本来就落后目标约 5cm，复现这个落后量才复现出动作。任何"消除跟踪误差"
  的优化都会把手臂推离源演示走过的路。**改一个量之前先问它是不是被依赖的。**

- **干预对准的是"事件"还是"subtask 边界"，先量两者差几帧**——三次驻留实验（第三、第五修法、
  方向二第一版）没有一次落在夹爪动作那一帧上，边界在夹爪动完 11~23 帧之后，源演示手臂那段走
  10~17cm。修正向量、驻留位置都要锚在事件帧。
- **一个量的全局中位数不是它在关键帧上的值**——"目标领先实际 5cm"是全帧中位，夹爪动作帧上是
  0.00cm，据此推出的"追上目标就是推离源演示"在关键帧上反了。关键帧单独量。

## 本主线状态（自动导入）

@docs/mainline2/STATE.md
