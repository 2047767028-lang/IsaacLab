# IsaacLab 项目工作准则

> 本文件记录 Claude Code 在本项目中应遵守的工作准则和用户偏好，
> 从 `genie_sim` 项目的 `CLAUDE.md` 中提炼而来（那份文件里项目专属的进度/bug记录未搬运）。

## 项目目标

用 **Isaac Sim + Isaac Lab** 操纵**两台 Franka 机械臂**，做**家具装配**任务。

路线：**遥操作采集少量真实演示数据** → 用 **MimicGen（isaaclab_mimic）系列工具** 把少量数据
放大成大量数据 → 喂给**策略训练**。

## 主线状态（2026-08-21 更新，下次对话先看这里）

### 主线一：双臂 Franka 家具装配（原始项目目标，尚未真正开始）

**当前进度**：目前跑通的是官方单臂 `Isaac-Stack-Cube-Franka-IK-Rel-v0`（叠方块任务）的
record → annotate → generate 全流程，用的是**官方自带的 10 条源演示**，不是自己遥操作
采出来的数据；也还是**单臂**，不是双臂家具装配。细节见 `RUNBOOK_mimic.md`。
双臂 Franka 环境和家具装配任务/资产目前在本项目里还没有配置。
自己遥操作那次（`my_demos.hdf5`）文件损坏/未录成，还没补录。

### 主线二：接触锚定的扰动增广（研究想法验证，进行中）

设计文档：`docs/接触锚定扰动增广_设计记录.md`（完整设计记录，含机制推导、风险清单、写作定位）。
一句话：验证 DP 策略对"视觉伺服交接给它那一瞬间"的初始位姿偏差的容忍边界，
方法是在 MimicGen 生成时给自由空间段加位姿扰动、接触段完全冻结。

**实验对象**：官方单臂 `Isaac-Stack-Cube-Franka-IK-Rel-Mimic-v0` + 它自带的10条官方源演示
(`datasets/annotated_dataset.hdf5`)——**跟主线一是同一个基础任务**，但跟主线一的最终目标
（双臂家具装配）无关，只是拿这个现成、已跑通的单臂环境做方法验证。

**关键实现发现（不用新写扰动逻辑）**：MimicGen 生成时第一个 subtask 的插值起点直接来自
`env.get_robot_eef_pose()`（钩子：`isaaclab_mimic/datagen/data_generator.py:538`
`merge_eef_subtask_trajectory`），而 reset 阶段已经有一个现成的机械臂关节高斯噪声事件
`randomize_franka_joint_state`（定义在
`isaaclab_tasks/.../stack/config/franka/stack_joint_pos_env_cfg.py` 的 `EventCfg`）——
把它的 `std` 参数化即可让扰动天然流入生成链路，完全不用碰 MimicGen 核心代码。

**分支**：`perturbation-experiment`（从 v2.3.2 tag 切出，因为本机装的是 Isaac Lab 2.3.2，
不能切去 `release/3.0.0-beta2`；仓库当时是分离头指针状态，切分支是为了让自动提交有安全落点）。

**已提交**：
- `467931872`：新增 `franka_stack_ik_rel_perturbed_mimic_env_cfg.py`
  (`FrankaCubeStackIKRelPerturbedMimicEnvCfg`，`PERTURB_STD` 环境变量控制扰动幅度)
  + 在 `isaaclab_mimic/envs/__init__.py` 追加注册新 gym id
  `Isaac-Stack-Cube-Franka-IK-Rel-Mimic-Perturbed-v0`（未改动任何已有注册）
  + `scripts/imitation_learning/isaaclab_mimic/sweep_start_pose_perturbation.py`（sweep脚本）。
- `6e5bd3404`：新增 `PERTURB_SEED` 环境变量控制 `datagen_config.seed`，配合复现性重跑用。
- `8c8cd05a0`：新增 `PERTURB_FIXED_ATTEMPTS` 环境变量——见下面"已纠正的错误"，
  这是修复"低成功率σ可能无限重试"隐患的关键改动。
- `1303ff823`：重写 sweep 脚本，支持按 seed 分批、每点落盘立即 fsync 写CSV、
  断点续跑（靠重新打开 hdf5 数 demo 数核实，不信任退出码）。

**已测速 + 健全性检查（通过）**：σ=0.02（现有默认值）测出成功率 48.4%/42.3%，
跟已有基线（官方10条演示直接生成，20/41≈48.8%）同量级，没有跑偏。
耗时模型（实测拟合）：`时间 ≈ 13.2s（固定进程开销）+ 0.395s × 总尝试次数`。

⚠️ **已纠正的错误（2026-08-21，教训要记住）**：CLAUDE.md 曾在这里写过
"`max_num_failures=25` 硬上限兜底，σ 崩到接近0%成功率也不会拖慢"——**这是错的**，
当时只读了 `DataGenConfig.max_num_failures` 的文档字符串就当结论用，没去验证执行代码。
实测 σ=5.0 探测跑到337次尝试才凑够10个成功，才发现 `max_num_failures`
在 `isaaclab_mimic/datagen/generation.py` 的 `env_loop()` 里**从未被读取**——
全代码库搜索，18个mimic env cfg 都赋值了这个字段，但没有一处真正拿它做判断；
真正的终止条件只有"凑够 `generation_num_trials` 个成功"（默认 `generation_guarantee=True`），
低成功率σ会无界重试。已用 `PERTURB_FIXED_ATTEMPTS` 修复（设置后把 `generation_guarantee`
切成 `False`，`generation_num_trials` 语义变成"固定跑多少次尝试"，不再依赖成功率）。
**教训**：配置字段的文档字符串描述的是"设计意图"，不是"实际生效的行为"，
必须去读真正执行判断的代码，这条本身就是"取证与验证纪律②"的一个实例。

⚠️ **还会复发的坑：长耗时的 `run_in_background` Bash 任务会被莫名其妙杀掉（2026-08-21）。**
现象：跑了约61分钟的 sweep wrapper 进程被杀（不是脚本报错、不是OOM、不是磁盘满，
`dmesg`/`journalctl` 查不到痕迹），子进程孤儿化后被 `systemd --user` 收养、继续跑但没被杀。
排查过：不是固定时长上限（后来一个存活远不到1小时的极简等待循环也被杀，
但同时间持久 `Monitor` 没被杀）——推断是**专门针对 Claude Code 自己 track 的
`run_in_background` 类型 Bash 任务**，具体机制未查实（查过官方文档，没有写明这条）。
**解法**：发起长任务时用 `setsid ... < /dev/null &` + `disown`，让整个进程树在OS层面
彻底脱离当前session（可用 `ps` 查 `SID` 是否等于自己的PID来验证是否真的脱离成功），
不再依赖 Claude Code 对该任务的存活追踪，改用自建的 `Monitor` 盯日志 + 定期 `pgrep`
心跳双重覆盖。⚠️ 附带发现一个独立的坑：**孤儿化的子进程会真的卡死**
（吃CPU但输出文件不再增长），根因猜测是 `subprocess.run(..., capture_output=True)`
用 Python 管的 PIPE 接子进程输出，父进程被杀后 PIPE 读端消失，子进程写日志时
可能卡在处理不了的 SIGPIPE/EPIPE 上——**解法是别用 `capture_output=True`，
把子进程 stdout/stderr 直接重定向到打开的日志文件**（没有"读端会消失"这回事）。
两层解法都已应用在 `scripts/imitation_learning/isaaclab_mimic/sweep_start_pose_perturbation.py`
（`setsid`部分是发起时的shell命令，不在脚本里；capture_output部分见 commit `cac8766d9`）。

**最终扫描配置**（2026-08-21 用户拍板"方案B加密扩大 + 复现性重跑 + 尽量拉大时间"后定案）：
20个σ值（0.00～8.00，低段密、高段覆盖塌陷区）× 5个独立seed × 每点固定500次尝试，
预计 5.85 小时，留 2.15 小时缓冲。测速阶段顺带探出粗略形状：
σ=0.02→~45%、0.15→43.5%、0.5→33.3%、1.0→24.4%、2.0→8.9%、3.0→5.7%、5.0→2.9%、8.0→3.0%
——衰减比预期平缓，5.0和8.0已持平，说明塌陷区在这附近趋稳。

**当前状态**：✅ 正式扫描已跑完（2026-08-22凌晨，实际耗时约7小时，在8小时预算内），
数据已交付，结果记录在 `docs/接触锚定扰动增广_实验记录_v1.md`
（+ `docs/perturbation_sweep_v1_results/`：`combined.csv`100行原始记录、
`aggregate.csv`跨seed汇总、`success_rate_vs_sigma.png`曲线图，均已 commit `469bab130`）。

**测到的形状（只是描述，不是结论——按设计文档"当仪器不当卖点"的定位，见下面成功判据）**：
σ=0.00~0.40 成功率持平在 36~37%（跨5个seed差异<1个百分点，本实验内部基线，
不是早期n=41的48.8%——那个小样本置信区间宽达±15%，已被更精确的n=500数据取代）；
σ=0.5~1.6 快速下降（32.8%→10.8%）；σ≥2.0 变缓，σ=5.5/8.0 趋稳在1.6~1.8%。

⚠️ **σ 的物理含义（2026-08-22 澄清，之前口头说过"含夹爪"是错的，已订正）**：
σ 是加在机械臂**7个手臂关节**（不含夹爪，夹爪会被重设回默认值）上独立高斯噪声的标准差，
**单位是弧度**，不是末端执行器的笛卡尔位移，两者不能直接换算（取决于哪个关节动、当前臂型）。
实测末端执行器实际位移（200并行环境，reset后立刻读末端位姿，σ=0.0验证方法干净、
位置标准差仅4e-7米）：**σ=0.2→均值17.3cm；σ=0.4→均值35.1cm**。
也就是说"成功率几乎不掉"的σ=0~0.4平坦区间，末端执行器已经覆盖了0～十几厘米量级的
初始位姿误差，不是微扰——这对应用到"视觉伺服交接容忍边界"这个原始研究问题时是有意义的量级。

**过程中两次异常均已排查+修复+记录在结果文档"执行过程中的异常"节**：
①`run_in_background`长任务约61分钟被莫名杀掉（已用`setsid`detach修复，见上面
"还会复发的坑"）；②孤儿进程本身会卡死（SIGPIPE/EPIPE猜测，已把子进程输出
从`capture_output`管道改成直接写文件，commit `cac8766d9`）。修复后84个点零复发。

**下一步（未做，留给用户决定）**：这条曲线怎么用——比如"生成模式"该把σ定在哪个值、
要不要在0.5~1.6陡降区间加密重测——是第4节"成功判据"里第一层的应用决策；
真正验证"扰动增广是否让训练数据更好"还是要看第二层（LeRobot训DP/ACT+仿真测评），
目前完全没开始。

### 主线二·扩展到全部subtask轨迹多样化（2026-08-22，设计中，还没实现）

**触发原因（用户发现的结构性局限，不是bug）**：`subtask_trajectory`（被插值的目标轨迹）
只由物体位姿决定，跟起点扰动完全无关，插值（`num_interpolation_steps=5`）一结束就
完全接回原版MimicGen的固定轨迹。**不管扰动1个subtask还是全部4个，多样性只存在于
每段开头约5帧，后面90%+的帧跟不加扰动时逐帧相同**——只扰动起点这个设计从结构上
就做不到"轨迹本身不一样"。

**中途踩出的两个bug（已定位根因，新方案靠换实现方式绕开，不是靠修复）**：
1. `get_robot_eef_pose()`读的是`obs_buf`观测缓存，不是实时物理状态；缓存靠`ee_frame`
   这个`FrameTransformer`传感器供数，传感器只有被显式标记"过期"（`sensor.reset()`）
   才会在下次访问时重算，直接写关节状态不会触发这个标记。
2. **更严重**：`write_joint_state_to_sim`是纯运动学瞬移，不经过物理步进，摩擦力约束
   没有时间去传递，**手里抓着的方块不会跟着瞬移，等于凭空脱手**——这才是σ=0.02（全
   subtask版）成功率暴跌到16%、σ=0.2暴跌到0%的主因，不是"扰动幅度大导致难恢复"。

**新方案设计（2026-08-22 用户定案）**：
- 不再碰物理仿真实时状态（绕开上面两个bug），改成**纯数据层操作**：从`subtask_trajectory`
  （原本固定的路点序列）中间取几个点加偏移，重新插值缝回去，让整条轨迹保持平滑、
  机器人真实物理连续执行（手里的东西自然跟着走，不会被瞬移甩掉）。
- **关节空间 vs 末端位姿空间，两条路线都记录，用户选了后者**：
  路线A(关节空间，需IK/FK往返)：优点是保证扰动后姿态一定关节可达；缺点是IK可能不收敛、
  多一层复杂度。**已讨论，暂不采用**。路线B(末端位姿空间，直接偏移`Waypoint.pose`，
  不用IK)：**用户选定，先做这个**，实现更简单。
- **关键设计约束（用户定的，不是可选项）**：
  ① **偏移方向必须共享/一致，不能每个点独立随机加噪声**——独立噪声会让路径变成
  "弯弯曲曲"的锯齿形；要的是整条被扰动段朝同一个方向平滑弓出去，像画一条弧线。
  ② **尾部必须跟原轨迹重合**——越接近该subtask的交互目标（抓取/放置点），插入扰动点
  的密度/幅度越低，最终收敛回原始轨迹，保证交互那一刻的稳定性。跟设计文档3.2节
  `σ(t)=σ_max·f(d(t))`是同一个思路。

**实现 v1 → v2 的完整过程（2026-08-22，已验证，已定型）**：

- **v1(离散控制点+直线插值，已废弃，git revert 0d2d676a2→420f7d890)**：从固定轨迹里取几个
  控制点加偏移（sin包络），直线连回去。40次固定尝试测出成功率仅**2.5%**，用户追问
  "尾部明明收敛了，为什么成功率还这么低"，逐帧算"转向角"（相邻帧速度方向夹角）实测
  确认：**位置数值确实收敛到0，但控制点之间是直线段，在每个控制点处（尤其弧线顶点）
  会有真实的方向硬转折**——frame 28 测出 116.9°的瞬间转向（基线同一帧只有29.8°），
  跟位置有没有对上坐标无关，是路径本身有没有棱角的问题。`sin(πu)`包络本身也只在
  两端**数值**为0，**斜率**不为0，会在收敛边界再叠加一次速度台阶。
- **关键澄清（用户提出"调小幅度+调高密度"，讨论后确认这不是另一个方案，是同一个
  方案的极限**：直线段拼出光滑曲线，靠"分段越多越逼近"，密度推到极限（每一帧都是
  控制点）就自动退化成"逐帧连续套函数"——v1的改良版和v2是同一个终点。**但密度和幅度
  不独立**：多大密度算够取决于幅度，不能拿(密度,幅度)做自由二维网格扫描，低密度+
  高幅度那个角必然还是有棱角，扫了也白扫。所以直接把密度推到理论极限，从根上消除
  "密度够不够"这个问题。
- **v2(逐帧连续套`sin²(πu)`包络，已实现，commit `4ec4685e2`)**：不再有任何控制点/
  直线段，自由区间每一帧都直接算包络值。`sin²`比`sin`多一层保证——两端**数值和斜率
  都是0**，收敛边界不会有速度台阶。`PERTURB_ARC_NUM_POINTS`参数已删除（密度概念不再
  存在），只保留`PERTURB_ARC_STD`（幅度，米，>0即启用）和`PERTURB_ARC_FREEZE_FRAC`
  （冻结尾部比例，默认0.3）两个可调项。
- **验证结果（同一seed、同一幅度std=0.05m=5cm，v1 vs v2 对照）**：40次固定尝试，
  **v1成功率2.5%(1/40) → v2成功率10.0%(4/40)**，方向正确、有实质改善，但n=40样本量小，
  统计上还不算铁证（粗略双比例检验z≈1.4，约90%置信但不到95%）——需要后面正式扫描
  用更大样本量确认。10%这个量级本身也提示：即便棱角问题解决了，"4个subtask全部叠加
  几厘米量级弧线扰动"本身仍然是不小的代价（粗算单subtask若约55~60%成功率，四次方
  0.55^4≈9.2%，跟测到的10%量级吻合）——接下来更可能靠**调小幅度**换成功率，而不是
  还有实现层面的棱角问题要修。

**下一步（未做）**：先做小规模探测（多个更小的std值，比如0.5~3cm这个区间），摸清楚
成功率还过得去的幅度范围，再决定要不要做正式大规模扫描——不能直接照搬第一次实验
（只扰subtask1起点）测出的σ量级，这次是全subtask都在动，代价结构完全不同。

**用户提出的关键疑问：ACT这类模型会不会把不同轨迹"取均值"导致白费功夫？**
- 讨论结论（未经实测，仅推理）：**大概率不会完全白费，但不是零风险**。每条被扰动轨迹
  对应的是真实不同的状态（机械臂物理上就在那个偏移位置），动作从这条新轨迹本身重新
  导出（跟设计文档2.3节"情况二"一致，自洽）——不是"同一个状态硬贴两个不同动作标签"，
  经典的"平均导致穿墙"问题大概率不会出现。
- **真正风险集中在收敛区**：多条弧线都要收敛回同一条尾部轨迹，如果收敛得晚/急，
  state space上会有重叠，重叠区里"正确动作"可能因来向不同而不同——上面①②两条约束
  （方向一致+密度衰减）本身就是在压窄这个重叠区，两件事是一体的，不是两个独立要求。
- **ACT不是纯MSE回归**，是CVAE+L1，理论上能靠隐变量z区分模式，但部署时z通常不采样，
  能不能真正分开取决于encoder学得好不好，不确定——大概率介于"纯回归会被平均坏"和
  "Diffusion Policy能吃下多模态"之间。**这正是设计文档4.4节"多模态对下游架构敏感"
  提前预判过的点**：这个增广对DP、ACT效果很可能不一样，值得同时训两个对比着看，
  本身就是有意义的发现，不是要回避的风险。**最终还是要靠训练+仿真测评本身回答，
  现在只是降低"这个方向完全没用"的担忧，不能替代实测。**

**小规模探测（2026-08-22，11点，每点50次固定尝试，freeze_frac=0.3固定）**：
0.5cm→34.0% / 0.7→34.0% / 1.0→40.0% / 1.2→34.0% / 1.5→30.0% / 1.8→26.0% / 2.0→28.0% /
2.5→18.0% / 3.0→14.0% / 4.0→12.0% / 5.0→10.0%。形状：0.5~1.2cm平台区（贴基线~37%，
差异在噪声内）；1.2cm后温和下降；2.5cm往后陡降。

**正式大规模扫描（2026-08-22完成，用户拍板"优先精度"后定案）**：
15个幅度值（cm，密集段0.5~1.5步长0.1，稀疏延伸段1.8/2.0/2.5/3.0）× 5 seed ×
每点500次固定尝试 = 37500次生成尝试。实测耗时235~238秒/点（比预算模型的210.7s
高约13%，弧线扰动逐帧算包络比v1单纯reset噪声多算一点，已如实记录），4个seed间
耗时无上涨趋势，总耗时约4.9小时，6小时预算内留够缓冲（未跌破30分钟警戒线）。

**结果（跨5seed汇总，commit `1cb5b05e0`，`docs/perturbation_sweep_v2_arc_results/`）**：
0.5cm→35.1% / 0.6→34.9% / 0.7→34.9% / 0.8→34.4% / 0.9→33.8% / 1.0→33.1% / 1.1→32.6% /
1.2→33.3% / 1.3→32.4% / 1.4→31.6% / 1.5→30.1% / 1.8→28.8% / 2.0→27.8% / 2.5→23.9% /
3.0→19.1%。0.5~0.9cm缓降，1.0~1.2cm有小幅波动（在跨seed标准差1.6~1.8pp以内，判断是
噪声不是真实回升），1.5cm后降幅明显加大。**3.0cm处仍在下降，没有摸到像v1（起点扰动）
实验那样"塌陷后企稳"的平台**——如果以后要往更大幅度补点，这个网格还没覆盖到那个区域。
派生数字（数学推算非实测）：假设4个subtask独立，单subtask隐含成功率≈合并成功率^(1/4)
≈74~77%，仅供参考，独立性假设本身没验证过。

数据完整性已独立核对（8个随机点重开hdf5核对demo数，8/8一致；`combined.csv`恰好75行
无重复无缺失）。全程零卡死零重跑，复用了v1踩坑后的全套防护（setsid、子进程输出写文件
不用PIPE、Monitor+pgrep心跳）。

**当前状态**：✅ 正式扫描已跑完，数据已交付，结果文档在
`docs/接触锚定扰动增广_实验记录_v2_arc.md`。⬜ 下一步（未做，留给用户决定）：
这条曲线怎么用（比如"生成模式"该定在哪个幅度）、要不要往更大幅度补点找平台、
或者直接推进到第二层验证（LeRobot训DP/ACT+仿真测评）。

### 主线二·峰值位置可变（v3，2026-08-22 用户提出，已实现，未跑规模验证）

**想法**：v2的`sin²(πu)`包络峰值固定在自由区间正中央（u=0.5），首尾两点（自由区间
起点/终点）之间的直线距离是固定的（不受扰动影响，因为这两点从来没被碰过）。**但峰值
出现在自由区间的什么位置，是一个被浪费掉的自由度**——峰值可以偏早（刚离开起点就鼓出去，
然后长距离缓慢收回）或偏晚，不一定非要对称。同样的"幅度+方向"参数下，能多产出一批
路径形状不同的轨迹，是幅度、方向之外的第三个多样性维度。

**实现（commit `1706bfffc`，`/btw` 命令自动fork了一个后台任务实现的，跟我原本记录的
"双段sin²拼接"数学构造不是同一个，但同样成立）**：包络从固定对称的`sin²(πu)`泛化成
`u^a·(1-u)^b`族（阶数a+b=6），峰值位置`u*=a/(a+b)`由新增的
`PERTURB_ARC_PEAK_FRAC_MIN`/`PERTURB_ARC_PEAK_FRAC_MAX`两个环境变量控制范围，
每次调用独立采样。内部把峰值位置钳制在(1/6, 5/6)，保证a>1、b>1，从而维持v2确立的
"两端数值和导数都精确为0"这条硬约束，不会重新引入v1的转折问题。默认(0.5,0.5)——
锁定中心，行为向后兼容（但函数族已经换了，跟v2不是逐字节相同的曲线，只是端点性质
和峰值位置的定性行为一致）。

**验证情况**：只做了纯数学验证（峰值位置在几个取值下精确命中、端点数值和斜率趋近0），
**没有跑Isaac Sim端到端生成验证成功率影响**——这是有意为之，超大规模仿真扫描该由
主协调者决定何时投入算力，不属于这次一次性实现指令的范围。**下一步如果要用，需要
先跑小规模探测**（类似v1/v2走过的路：固定/浮动峰值对成功率的影响，跟单纯"幅度"这个
维度是否有交互），确认没有引入新的意外之后再考虑要不要并入下一轮正式扫描。

## 主线二的成功判据（2026-08-21 用户定义，两层，别混着说）

**第一层（生成阶段的门槛，现在这轮扫描测的就是这个）**：加了位姿扰动后，
在某个σ区间内生成成功率跟原版 MimicGen（不加扰动）差不多——说明加扰动没把数据搞坏
（留下来的每条演示仍然真实通过任务成功判据），能拿到"够多、能用"的数据。
**这一层只是前置门槛，不是最终结论**——它只能证明"没亏"，不能证明"更好"。

**第二层（真正验证假设的地方，还没跑）**：把这批数据喂给 LeRobot 训 DP/ACT，
再回仿真里测评，看成功率/鲁棒性是不是真的比"不加扰动的原版 MimicGen 数据"更好。
**"扰动增广的数据基本质量更高、更适合训练"这句话，要等第二层跑完才能下结论，
现在这轮σ扫描曲线不能替代它。** 中途任何报告都不该把第一层的结果说成第二层的结论。

**曲线出来后的下一步**：从曲线里挑一个"生成成功率接近基线"的 σ 作为正式操作点，
用它生成够量的训练数据——如果挑中的σ在扫描里成功率还不错（比如~30%+这个量级），
**扫描本身产出的成功demo可能就够直接用**（500次×30%×5seed≈750条量级），
不一定要再单独跑一轮"凑够N个成功"模式；具体够不够，等曲线出来看数字再定。

⚠️ **这条主线的执行方式（用户当场定的规矩）**：即使是 goat 模式/自动化运行，
每步源码/脚本改动仍必须单独 `git commit`，不许因为无人盯着就放宽或攒批提交。

## 改动纪律

- **源码/功能改动（`.py`/`.sh`/`.yaml`/`.json` 等）→ 改完就 `git commit`**，
  提交信息写清"改了什么、为什么"。
- **纯文档/注释改动（如 `CLAUDE.md`）→ 不必每次都提交**。
- **不用"复制到 backups/ 目录"当改动前的保护**——备份靠人肉比对容易出错、越堆越乱，
  且用户没法拿它 `git diff`。改动前该做的是确认工作区干净、需要时用 git 本身的机制
  （stash/commit/diff），git 记录精确到行，比手工备份可靠。

## 取证与验证纪律

前提：结论必须能落到**用户自己能核对的数字，或者自己能跑的命令**上，
不能停留在"听起来对"的层面。

**① 精确引用/需要写进正式产物的原文，用 `curl -o` + `grep`，不用 `WebFetch` 转述。**
```bash
curl -sfL -o <文件> <raw链接>      # -f: 404 时失败，避免把错误页当源码存下来
grep -qF "<要核对的原文>" <文件>    # -F: 当纯文本匹配，避免代码里的括号/点被当正则
```
`WebFetch` 拿到的不是原文，是"抓取 + 小模型转述"，用于精确引用会走样。
⚠️ 用 `-o` 写盘、不进上下文；裸 `curl`/`cat` 大文件才是上下文暴涨的真凶。
渲染页面或只需要"确认存在"这类弱需求，用 `WebFetch` 没问题；
需要精确原文（尤其是要引用到公开产物里）时才必须换成 curl+grep。

**② 验证优先级：能推算的推算 → 能跑测试的测 → 实在不行才做重量级的实际运行/仿真**
（这类验证通常耗时且成本高，作为最后手段）。
⚠️ 最需要警惕的情况：既没有可验证的数字、也没有可运行的验证手段——
这种结论必须明确标成"未验证的推测"，不能当作已确认的结论使用。

**③ 自查话术红灯**：出现"逻辑上应该没问题"/"参考了官方实现"/"这样改更合理"这类
缺乏实测支撑的表述时，应该先自问（或反问用户）**"这个改动在什么情况下会出错？"**，
把结论落实到具体场景，而不是停在直觉判断上。

**④ 子 agent 的定位是"唱反调的第三方视角"，不是事实核查工具。**
子 agent 和主 agent 容易共享同类系统的失效模式，"另一个 AI 说没问题"不构成证据；
用它来提供独立视角/挑刺，而不是用它的结论替代实测。

## 沟通风格偏好

- **默认使用中文交流**（代码、注释、变量名等技术产物仍用英文/项目原有语言，
  沟通说明用中文）。
- **做判断、排查、取舍时，展示推理过程而不是只甩结论**：讲清"考虑了哪些选项、
  为什么选这个、什么情况下会不对"，让用户能在过程中发现问题及时纠正，
  而不是等到最终结论或代码改完才能审查。
- **边界**：纯执行性操作（读文件、跑已经谈妥的命令、按已定方案编辑代码）
  不需要展开讲；只在真正存在判断/取舍/不确定性的节点才展开推理过程——
  否则会退化成流水账，反而埋没真正的决策点。
